var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API","text":"Exported symbols from HomotopyGetsReal. Docstrings are taken from the package sources (Phase 7 docstring audit); no additional narrative is defined here.","title":"API reference"},{"category":"section","location":"api/#Configuration-and-types","page":"API","text":"","title":"Configuration and types"},{"category":"section","location":"api/#Clustering","page":"API","text":"","title":"Clustering"},{"category":"section","location":"api/#Critical-points-and-bounding-objects","page":"API","text":"","title":"Critical points and bounding objects"},{"category":"section","location":"api/#Isosingular-deflation","page":"API","text":"","title":"Isosingular deflation"},{"category":"section","location":"api/#Path-tracking","page":"API","text":"","title":"Path tracking"},{"category":"section","location":"api/#Curve-topology","page":"API","text":"","title":"Curve topology"},{"category":"section","location":"api/#Face-tracking","page":"API","text":"","title":"Face tracking"},{"category":"section","location":"api/#Projection","page":"API","text":"","title":"Projection"},{"category":"section","location":"api/#Surface-decomposition","page":"API","text":"","title":"Surface decomposition"},{"category":"section","location":"api/#Visualization","page":"API","text":"","title":"Visualization"},{"category":"type","location":"api/#HomotopyGetsReal.HomotopyConfig","page":"API","text":"HomotopyConfig{T<:AbstractFloat}\n\nCentral configuration for numerical tolerances and algorithm knobs.\n\nAll pipeline steps read settings from a single HomotopyConfig{T} so they can be tuned in one place and instantiated at a chosen floating-point precision T (e.g. Float64 or BigFloat). Numeric defaults use T(...) so HomotopyConfig{BigFloat}() gets full-precision literals.\n\nFields\n\nTolerances\n\ncritical_point_tol: Residual tolerance for accepting a homotopy endpoint as a critical point.\nboundary_point_tol: Distance tolerance for classifying points on the bounding-box boundary.\nvertex_match_tol: Distance tolerance for merging duplicate vertex coordinates.\njacobian_rank_tol: Singular-value cutoff when computing numerical Jacobian rank.\nsingular_value_threshold: Threshold for classifying a vertex as Singular.\npath_tracker_precision: Requested precision for HomotopyContinuation path tracking.\npatch_transversality_cos_tol: Cosine threshold for re-anchoring skewed z-sweep patches.\nprojection_orthonormality_tol: Acceptance threshold for norm(Q'Q - I) when validating a user-supplied projection matrix.\n\nKnobs\n\nmax_path_steps: Maximum path-tracker steps before declaring failure.\nbbox_x, bbox_y, bbox_z: Bounding-box (min, max) extents along each axis.\nedge_sample_density: Number of points sampled per edge in sample_edge.\nmidslice_sample_density: Sample count when building a mid-slice mesh.\nz_mid_retry_frac: Fraction of slab width used to perturb z_mid on retry.\nmax_z_mid_retries: Maximum perturbed-z_mid attempts in _robust_slice_at_z.\nz_mid_gradient_ratio_tol: Minimum patch-direction strength ratio for accepting a slice.\nmin_slab_width: Resolution floor for z-slab boundaries; closer critical z-values are merged.\nincidence_snap_tol_ratio: Chord-error-aware distance ratio gating Phase 9b snap-unification/continuity.\nisosingular_verify_retries: Retry budget for verify_isosingular_dimension's fresh-hyperplane attempts.\nmax_deflations: Round budget for resolve_isosingular_dimension's deflation attempts.\n\n\n\n\n\n","title":"HomotopyGetsReal.HomotopyConfig"},{"category":"type","location":"api/#HomotopyGetsReal.VertexType","page":"API","text":"VertexType\n\nClassify a NativeVertex by how it was produced.\n\nCritical: critical point of the projection map.\nBoundary: intersection with the bounding object.\nSingular: rank-deficient Jacobian at the point.\nArtificial: synthetic vertex from merging or slicing.\n\n\n\n\n\n","title":"HomotopyGetsReal.VertexType"},{"category":"type","location":"api/#HomotopyGetsReal.NativeVertex","page":"API","text":"NativeVertex{T<:AbstractFloat}\n\nA vertex of the numerical decomposition with complex coordinates.\n\nVertices are referenced by id from Edge and Face. Coordinates live in complex space because homotopy paths are tracked in ℂⁿ.\n\nFields\n\nid: Unique identifier.\ncoordinates: Vertex coordinates as Vector{Complex{T}}.\nv_type: Classification; see VertexType.\nmetadata: Per-vertex diagnostics and bookkeeping.\n\n\n\n\n\n","title":"HomotopyGetsReal.NativeVertex"},{"category":"type","location":"api/#HomotopyGetsReal.Edge","page":"API","text":"Edge{T<:AbstractFloat}\n\nA path segment between two vertices with sampled points along it.\n\nRaw samples come from connect_the_dots!; sample_edge refines spacing.\n\nFields\n\nid: Unique edge identifier.\nleft_vertex_id, right_vertex_id: Endpoint NativeVertex ids.\nsampled_points: Real-space samples as Vector{Vector{T}}.\nis_singular: Whether the edge passes near a singular locus.\n\n\n\n\n\n","title":"HomotopyGetsReal.Edge"},{"category":"type","location":"api/#HomotopyGetsReal.Face","page":"API","text":"Face{T<:AbstractFloat}\n\nA 2D decomposition cell from a mid-slice with boundary edges and mesh data.\n\nFields\n\nid: Unique face identifier.\nmid_slice_z: Z-coordinate of the source mid-slice. When decompose_3d_surface runs with a projection, this is the sweep coordinate of the rotated working chart, not literal world z.\nboundary_edges: Ids of bounding Edges.\nmesh_vertices: Mesh vertex coordinates, one row per vertex.\nmesh_topology: Mesh connectivity indices, one row per element.\n\n\n\n\n\n","title":"HomotopyGetsReal.Face"},{"category":"function","location":"api/#HomotopyGetsReal.cluster_vertices","page":"API","text":"cluster_vertices(vertices::Vector{NativeVertex{T}}, tol::T) where {T<:AbstractFloat}\n    -> Vector{NativeVertex{T}}\n\nCluster nearby vertices and return one representative per component.\n\nForm connected components under Euclidean distance ≤ tol on complex coordinates, then merge each cluster into a single NativeVertex. Representatives are sorted by id.\n\n\n\n\n\n","title":"HomotopyGetsReal.cluster_vertices"},{"category":"function","location":"api/#HomotopyGetsReal.cluster_scalars","page":"API","text":"cluster_scalars(xs::Vector{T}, tol::T) where {T<:AbstractFloat}\n    -> Vector{T}\n\nCluster nearby scalar values and return sorted cluster centroids.\n\nSort xs, merge consecutive values within tol, and return the arithmetic mean of each run in ascending order.\n\n\n\n\n\n","title":"HomotopyGetsReal.cluster_scalars"},{"category":"function","location":"api/#HomotopyGetsReal.cluster_points_indexed","page":"API","text":"cluster_points_indexed(points::Vector{Vector{T}}, tol::T) where {T<:AbstractFloat}\n    -> (representatives::Vector{Vector{T}}, membership::Vector{Int})\n\nCluster nearby points and return representatives plus a membership map.\n\nApply tolerance-based union-find on real coordinate vectors (same distance rule as cluster_vertices). Return centroid representatives and membership[i], the cluster index for each input point.\n\n\n\n\n\n","title":"HomotopyGetsReal.cluster_points_indexed"},{"category":"type","location":"api/#HomotopyGetsReal.VertexRegistry","page":"API","text":"VertexRegistry{T<:AbstractFloat}\n\nPhase 10: a PERSISTENT, cross-call vertex-identity substrate — unlike cluster_vertices, which deduplicates a single batch of vertices and then discards its own bookkeeping, a VertexRegistry accumulates vertices ACROSS MULTIPLE SEPARATE CALLS (one call per slab, plus one per shared crit-slice, in decompose_3d_surface's slab loop), assigning the SAME id to any two candidates that land within tol of an already-registered vertex — even though those candidates come from numerically INDEPENDENT computations (e.g. a slab's own decompose_1d_curve call and the shared crit-slice's own slice_at_z call, both converging on \"the same\" critical point at a shared z-boundary).\n\nThis is the gap cluster_vertices was explicitly anticipated to extend into (see this file's own header, written in Phase 2), but a genuinely different OPERATION from it: cluster_vertices clusters a known, complete Vector all at once (batch); VertexRegistry matches ONE new candidate against a growing store, one register! call at a time (streaming), with no upper bound on how many separate calls may eventually contribute to the same canonical vertex.\n\nStage 1 (this struct + register!): a pure, standalone data structure, exercised by its own unit tests (test/test_vertex_registry.jl).\n\nStage 2 (wired): decompose_3d_surface(...; incidence = true) constructs one VertexRegistry per call and routes every slab's and every _decompose_crit_slice's vertex ids through register! instead of the positional v_hwm offset scheme (see decompose_3d_surface's own Phase 10 comment). incidence = false never constructs one and stays on the offset scheme unchanged.\n\nFields\n\nvertices: canonical, deduplicated entries; vertices[id] is the entry with that id (ids are assigned sequentially as new vertices are registered, and are never reassigned once given out).\ntol: matching distance — reuse cfg.vertex_match_tol, the SAME \"are these the same vertex?\" identity tolerance cluster_vertices already uses, not a new one.\nraw_contributions: raw_contributions[id] accumulates every (candidate.id, candidate.metadata) pair ever merged into vertices[id] across all register! calls — kept explicitly (not folded into vertices[id].metadata in place) so merge_metadata can be RECOMPUTED FROM THE FULL accumulated set on every new match, rather than merging two-at-a-time. The latter would be lossy: merge_metadata's own :cluster_size/:cluster_member_ids bookkeeping is a batch operation (see its own docstring), and repeatedly overwriting an already-merged entry's metadata with a fresh 2-member merge would silently discard earlier members' contributions. Recomputing from the full raw list every time keeps the merge genuinely correct no matter how many separate register! calls eventually land on the same canonical vertex — verified directly in the test suite across 3+ incremental merges, not just the 2-item case.\n\n\n\n\n\n","title":"HomotopyGetsReal.VertexRegistry"},{"category":"function","location":"api/#HomotopyGetsReal.register!","page":"API","text":"register!(reg::VertexRegistry{T}, candidate::NativeVertex{T}) where {T<:AbstractFloat} -> Int\n\nRegister candidate against reg, returning the id it is assigned.\n\nFinds the NEAREST already-registered vertex within reg.tol of candidate.coordinates (not merely the first found within tolerance — matters for determinism when multiple existing entries could plausibly be in range). If one exists, candidate is MERGED into it (returning that entry's existing, unchanged id — ids are never reassigned once given out) and the entry's stored coordinates, type, and metadata are updated:\n\ncoordinates: running centroid over every contribution merged into this entry so far (mirrors cluster_vertices's own centroid rule).\ntype: Singular if either the existing entry or candidate is Singular; else the common type if they agree; else Artificial — the EXACT three-way rule cluster_vertices documents (a rank-deficient contribution must never be silently averaged away by an otherwise-regular one), reused here rather than re-derived, and holding across any number of successive merges, not just the first one.\nmetadata: merge_metadata, recomputed from the FULL accumulated raw_contributions for this entry (see the struct's own docstring for why this must be a full recompute, not an incremental two-at-a-time merge).\n\ncandidate.id itself is used ONLY as metadata provenance (fed into merge_metadata's :cluster_member_ids bookkeeping, exactly as cluster_vertices already treats input ids) — it plays no role in which id gets returned, since (unlike a one-shot batch clustering, where \"smallest input id\" is an arbitrary-but-consistent tie-break) a persistent registry must keep an already-assigned id STABLE regardless of what id a later caller's own, unrelated local numbering happens to use.\n\nIf no existing entry is within tolerance, candidate is registered as a brand new canonical vertex with a freshly assigned id (length(reg) + 1), storing its own coordinates/type/metadata unchanged.\n\n\n\n\n\n","title":"HomotopyGetsReal.register!"},{"category":"function","location":"api/#HomotopyGetsReal.jacobian_rank_info","page":"API","text":"jacobian_rank_info(F::System, point::AbstractVector, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> (rank::Int, singular_values::Vector{T})\n\nEvaluate the Jacobian of F at point and return its numerical rank and singular values.\n\nPass a HomotopyConfig to supply jacobian_rank_tol, the cutoff used when counting singular values as nonzero. Used by compute_critical_points and intersect_bounding_object; vertex-type classification also consults cfg.singular_value_threshold separately.\n\n\n\n\n\n","title":"HomotopyGetsReal.jacobian_rank_info"},{"category":"function","location":"api/#HomotopyGetsReal.compute_critical_points","page":"API","text":"compute_critical_points(F::System, cfg::HomotopyConfig{T};\n                          deflate::Bool = false,\n                          F_original::Union{Nothing,System} = nothing) where {T<:AbstractFloat}\n    -> Vector{NativeVertex{T}}\n\nFind and classify critical points of a polynomial system as NativeVertex records.\n\nCall with either a square 0-dimensional system (e.g. a pre-augmented curve critical-point system) or a single equation in three variables (a raw surface; the z-projection critical system is built internally). Solutions are path-tracked, classified as Critical or Singular, and deduplicated with cfg.vertex_match_tol.\n\nIsosingular deflation, Stage 4b (2026-07), diagnostic-only: when deflate = true, every Singular-classified candidate additionally gets resolve_isosingular_dimension run against F_original, and the result is stored in that vertex's metadata (:isosingular_verdict, :isosingular_dimension, :isosingular_deflation_sequence) – coordinates, v_type, and every other existing return value are untouched. false (the default) is the exact pre-Stage-4 code path – no extra solves, F_original unused and unchecked.\n\nStage 4c gate (2026-07): before running resolve_isosingular_dimension, this function also checks _deflation_applicable(F_original, x, cfg). This is necessary because v_type == Singular here is classified against Faug (this function's own local pre-augmented-or-auto-augmented system), which is a DIFFERENT matrix than F_original whenever the two differ – a point can be Faug-singular via a higher-order degeneracy (e.g. a projection fold) while F_original itself is perfectly regular there (its own bare Jacobian stays full rank), which is not a point deflate_once can process at all. Found via a real crash on the Taubin heart fixture's crit-slices (ArgumentError: deflate_once: minorSize=2 exceeds available rows), not invented speculatively – see _deflation_applicable, in its own docstring, for the full derivation, including why the naively-obvious gate (corank(F_original) > 0) does NOT work. When the gate returns false, the candidate's v_type and every other field are left exactly as they would be with deflate = false: only the deflation attempt is skipped, no isosingular_* metadata keys are added, and there is no reclassification. intersect_bounding_object does not need this gate; see its own docstring.\n\nF_original has NO default value usable for deflation, on purpose, and this is checked at runtime (Julia has no compile-time-conditional-mandatory keyword): deflate = true with F_original === nothing throws ArgumentError immediately, in EITHER branch below (the pre-augmented curve case and the auto-augmented surface case alike) – not just the branch where inferring F_original = F would happen to be wrong. This is deliberately stricter than \"default correctly where it's safe, guard only where it isn't\": the auto-augmented surface branch (ne==1,nv==3) could technically default F_original to F correctly (that IS the bare surface already), but the pre-augmented curve branch (ne==nv) cannot – F there is already Faug, and deflating against it would silently target the projection-critical system instead of the true bare curve, the exact distinction this whole feature's first investigation spent its effort establishing matters. Rather than let correctness depend on which branch a future caller happens to be in, no branch gets a silently-inferred default; every deflate = true caller states F_original explicitly, always. deflate = false (the default) never evaluates this guard at all, so no existing non-deflating call site is affected by its existence.\n\n\n\n\n\n","title":"HomotopyGetsReal.compute_critical_points"},{"category":"function","location":"api/#HomotopyGetsReal.intersect_bounding_object","page":"API","text":"intersect_bounding_object(F::System, cfg::HomotopyConfig{T};\n                            deflate::Bool = false,\n                            F_original::Union{Nothing,System} = nothing) where {T<:AbstractFloat}\n    -> Vector{NativeVertex{T}}\n\nFind curve–bounding-box intersection points and return them as NativeVertex records.\n\nF must define a plane or space curve (length(F.expressions) == nvariables(F) - 1 with two or three variables). Each variable is fixed at its cfg.bbox_* bounds in turn; real solutions inside the box are classified as Boundary or Singular and deduplicated with cfg.vertex_match_tol.\n\nIsosingular deflation, Stage 4b (2026-07), diagnostic-only: identical contract to compute_critical_points's own deflate/F_original – see that docstring for the full rationale. Note this function's own F is already the bare, never-augmented curve/surface equation (unlike compute_critical_points's pre-augmented branch), so a default of F_original = F would in fact always be safe here. It is intentionally NOT given one anyway: the same explicit, no-inferred-default rule applies uniformly to both functions, rather than one function being strict and the other permissive depending on which happens to be safe – consistency of the rule matters more here than the marginal convenience of a default this one function alone could have gotten away with.\n\nStage 4c gate – not needed here: unlike compute_critical_points, this function does NOT call _deflation_applicable before deflating. Its v_type is classified directly against F itself (jacobian_rank_info(F, full, cfg) with expected_rank = length(F.expressions)), and F is exactly what callers pass as F_original – never a separately-augmented matrix. v_type == Singular here therefore already means rank(J_F) < length(F.expressions), i.e. minor_size = rank+1 <= length(F.expressions), which is deflate_once's own precondition, guaranteed by construction rather than needing a separate runtime check.\n\n\n\n\n\n","title":"HomotopyGetsReal.intersect_bounding_object"},{"category":"function","location":"api/#HomotopyGetsReal.estimate_corank","page":"API","text":"estimate_corank(F::System, point::AbstractVector, cfg::HomotopyConfig{T};\n                 expected_rank::Int) where {T<:AbstractFloat}\n    -> Int\n\nIsosingular-deflation Stage 1 primitive (2026-07 investigation): the Julia/HC.jl analogue of BertiniReal's nullSpace corank estimate from isosingular_deflation (src/symbolics/isosingular.cpp:5-134, upstream bertini_real 1.9.0). BertiniReal gets this number from Bertini1's own witnessGeneration under TrackType: 6 (an internal numerical-rank/SVD test, not visible in bertini_real's own source); this reimplements the same quantity directly on top of jacobian_rank_info, which already computes a genuinely T-generic SVD-based rank.\n\ncorank = expected_rank - rank(J). expected_rank is a MANDATORY keyword, no default (2026-07 hardening, following a witness-slice-anchoring investigation): this function now serves two genuinely different, co-equal conventions with no single sensible default between them – intersect_bounding_object/_classify_vertex_type's \"full ROW rank == smooth point\" convention (expected_rank = length(F.expressions), meaningful on a bare, possibly-underdetermined curve/surface equation) versus deflate_once's \"full COLUMN rank == isolated point in ambient space\" convention (expected_rank = length(F.variables), the one confirmed correct against the Hauenstein-Wampler D_det construction). These coincide only when F is square (as Faug always is) and diverge for every bare, single-equation curve/surface – exactly what deflation is called on. A prior version of this function defaulted to the row-rank convention; that default was silently wrong for column-rank callers relying on it, discovered via a verification run whose own print statement used the default instead of matching deflate_once's internal choice. Every call site must now state its convention explicitly.\n\nNote the scope boundary: this is a purely first-order (single-Jacobian-evaluation) quantity. It cannot by itself distinguish singularities that share the same first-order corank (e.g. a node vs. a cusp both report corank 1 under the row-rank convention) – doing so needs either a further deflation iteration (appending the minor equations and re-evaluating) or higher-order data. This is expected behavior, not a bug – see the Stage 1 verification in test_solver.jl.\n\n\n\n\n\n","title":"HomotopyGetsReal.estimate_corank"},{"category":"function","location":"api/#HomotopyGetsReal.deflate_once","page":"API","text":"deflate_once(F::System, x0::AbstractVector, cfg::HomotopyConfig{T};\n              expected_rank::Int = length(F.variables)) where {T<:AbstractFloat}\n    -> (F_new::System, corank_new::Int)\n\nIsosingular deflation Stage 2 primitive (2026-07): a single deflation iteration, the Julia/HC.jl-native analogue of createMatlabDeflation/deflate_no_subst.m (src/symbolics/isosingular.cpp:315-478, matlab_codes/deflate_no_subst.m:110-173) – symbolic minor construction via HomotopyContinuation.jacobian/LinearAlgebra.det instead of shelling out to MATLAB/Python.\n\nAppends every minorSize-sized minor of jacobian(F) that is not the identically-zero polynomial (iszero(expand(det(...))) – a structural zero-polynomial test, NOT a numerical test at x0: by definition of rank, every minorSize-sized minor vanishes numerically at a corank-c singular point when minorSize = rank(J)+1, so filtering on value-at-x0 would exclude everything; BertiniReal's own filter, simplify(det(...)) ~= 0 in Matlab, is the same structural test, just via a different CAS). minorSize is derived from the current corank at x0 via estimate_corank (minorSize = expected_rank - corank + 1, matching isosingular.cpp:195's formula with no homogenizing-patch correction, since HC.jl works affinely).\n\nThrows ArgumentError if x0 is already full rank at F (corank 0 – nothing to deflate) or if minorSize exceeds F's available rows or columns (length(F.expressions) / length(F.variables)) – the latter is not a bug to work around, it is deflate_once telling you F is not yet a valid witness-point system.\n\ncompute_critical_points, decompose_1d_curve, and decompose_3d_surface may hand this function the BARE curve/surface equation directly – no intermediate slicing step is needed before Stage 3/4 wiring. See docs/DESIGN_NOTES.md §isosingular-deflation, Stage 2, for the investigation behind that conclusion (a since-retracted docstring note once claimed the opposite).\n\n\n\n\n\n","title":"HomotopyGetsReal.deflate_once"},{"category":"type","location":"api/#HomotopyGetsReal.IsosingularVerdict","page":"API","text":"IsosingularVerdict\n\nThree-way outcome of verify_isosingular_dimension – deliberately not a Bool. Collapsing \"confirmed non-terminal\" and \"no clean answer either way\" into a single false would hide a real distinction: see docs/DESIGN_NOTES.md §isosingular-deflation, Stage 3, for the measured false-positive rate that motivated keeping these separate.\n\nVerified: d is confirmed as the isosingular local dimension at this round – either trivially (d == 0, an already-isolated point) or via a slice-moving homotopy track that both completed successfully AND landed on a point satisfying the ORIGINAL system to within tolerance.\nNotTerminal: every retry attempt cleanly failed to track – no attempt succeeded, and none reported an inconsistent success either. Real, repeated evidence this round is not yet stabilized; keep deflating.\nInconclusive: any single attempt reporting tracker-success with a residual outside tolerance against the original system forces this outcome immediately, however many attempts remain in the retry budget and however many clean rejections surround it – one inconsistent result is itself the signal, not noise to retry away.\n\n\n\n\n\n","title":"HomotopyGetsReal.IsosingularVerdict"},{"category":"type","location":"api/#HomotopyGetsReal.VerifyResult","page":"API","text":"VerifyResult{T<:AbstractFloat}\n\nReturn type of verify_isosingular_dimension.\n\nverdict::IsosingularVerdict\nendpoint::Union{Nothing,Vector{Complex{T}}}: the verified point, set only when verdict == Verified; nothing otherwise.\nattempts::Int: retry attempts actually used (0 for the d == 0 trivial case).\ninconsistent_count::Int: number of attempts that reported tracker-success but failed the original-system residual check – a count, not just a flag, so a caller can see how bad the inconsistency was, not merely that it happened.\n\n\n\n\n\n","title":"HomotopyGetsReal.VerifyResult"},{"category":"function","location":"api/#HomotopyGetsReal.verify_isosingular_dimension","page":"API","text":"verify_isosingular_dimension(F_current::System, F_original::System, x0::AbstractVector,\n                               d::Int, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> VerifyResult{T}\n\nIsosingular-deflation Stage 3 primitive (2026-07): verifies whether d (the current corank of F_current at x0) is the TERMINAL isosingular local dimension, via a Julia/HC.jl-native analogue of Bertini1's isosingularDimTest (isosingular.c:328-450) and Hauenstein-Wampler's Algorithm 6.3 – a slice-moving parameter-continuation track, not a numeric-sequence pattern match (see _corank_plateau_hint for why that alone is insufficient, per Definition 5.18 and the paper's own f_{k,l} counterexample family).\n\nF_current and F_original are deliberately separate, both required, never inferred from one another: F_current (the accumulated, possibly already-deflated system) is what the moving hyperplanes get appended to for tracking; F_original (the bare, undeflated defining equation(s)) is what the final acceptance residual is checked against. Conflating these into one argument was exactly the class of bug this signature is designed to make impossible to write by accident.\n\nConstruction: build d generic real hyperplanes L_i(x) = sum_j a_ij*(x_j - x0_j) through x0 (random real Gaussian a_ij, matching the genericity precedent set by random_orthogonal_matrix, not complex – x0 is always real by construction here, coming from a real-classified vertex), append them to the FULL, UNMODIFIED F_current – no equation discarding, no R(f)=A*f randomization (both tried and rejected; see docs/DESIGN_NOTES.md, section isosingular-deflation, Stage 3). Tracks via a parameter homotopy from start_parameters = 0 (the hyperplanes' value at x0, by construction) to a fixed nonzero target_parameters.\n\nAcceptance requires BOTH conditions, checked explicitly and never silently combined into one boolean:\n\nThe tracked path's return_code == :success.\nThe tracked endpoint satisfies F_original to within cfg.critical_point_tol – checked against the ORIGINAL system, never F_current or the augmented tracking system.\n\nCondition 2 is not optional decoration – an implementation that checked only condition 1 would produce real false positives (measured; see design notes).\n\nRetries: up to cfg.isosingular_verify_retries attempts, each with a FRESH random hyperplane draw (per Hauenstein-Wampler's own stated reliability recommendation for Algorithm 6.3 – \"perform this test multiple times using different L and lambda\" – NOT Bertini1's literal same-draw retry, isosingular.c:389-397, already found unsound in fixed Float64 precision with no AMP escalation). The guaranteed invariant is attempts == 0 when d == 0 (the trivial short-circuit below); otherwise 1 <= attempts <= cfg.isosingular_verify_retries, not a fixed count – see cfg.isosingular_verify_retries's own docstring for why the default is kept generous.\n\nd == 0 short-circuits to Verified with no tracking at all.\n\nrng: the fresh hyperplane draw at each retry (randn(rng, d, nv)) accepts a caller-seedable RNG, matching the random_orthogonal_matrix/decompose_3d_surface convention elsewhere in this codebase, instead of silently using Random.default_rng() with no way for a caller to reproduce a specific run. Callers that need a deterministic verdict/attempts/rounds value for testing should pass a seeded rng (e.g. Xoshiro(1)).\n\nSee docs/DESIGN_NOTES.md §isosingular-deflation, Stage 3, for: the rejected equation-discarding/R(f)=A*f alternatives and their measured false-positive rates; the retracted \"always 1 attempt\" claim and the 10-trial measurement that corrected it.\n\n\n\n\n\n","title":"HomotopyGetsReal.verify_isosingular_dimension"},{"category":"type","location":"api/#HomotopyGetsReal.ResolveVerdict","page":"API","text":"ResolveVerdict\n\nOutcome of resolve_isosingular_dimension – the multi-round analogue of IsosingularVerdict, which only ever describes a single round.\n\nDeliberately uses Ambiguous, not Inconclusive, for its middle value – a first version of this enum reused Inconclusive verbatim from IsosingularVerdict, which silently collided with it (this is a single flat namespace with no submodule separation). See docs/DESIGN_NOTES.md \"The ResolveVerdict/IsosingularVerdict enum-naming-collision near-miss\" for the full account and the naming convention it established going forward.\n\nResolved: the isosingular local dimension was found – either trivially (corank reached 0) or via a verify_isosingular_dimension call that returned Verified.\nAmbiguous: some round's verification returned Inconclusive – propagated up immediately, not retried at the orchestration level (a single inconsistent round is a reason to stop and look, not to keep deflating past it).\nExhausted: cfg.max_deflations rounds were spent without ever reaching Resolved or Ambiguous – distinct from Ambiguous on purpose: this means the corank sequence never stabilized long enough to even trigger a verification attempt, not that a verification attempt gave a mixed signal.\n\n\n\n\n\n","title":"HomotopyGetsReal.ResolveVerdict"},{"category":"type","location":"api/#HomotopyGetsReal.ResolveResult","page":"API","text":"ResolveResult{T<:AbstractFloat}\n\nReturn type of resolve_isosingular_dimension.\n\nverdict::ResolveVerdict\nisosingular_dimension::Union{Nothing,Int}: set only when verdict == Resolved.\nendpoint::Union{Nothing,Vector{Complex{T}}}: set only when verdict == Resolved.\ncorank_sequence::Vector{Int}: the full d_0, d_1, ..., d_rounds trace – kept for diagnostics regardless of verdict, not just on success.\nrounds::Int: number of deflate_once calls actually performed.\nF_final::System: the system in force when the loop stopped (F_original itself if rounds == 0). Not consumed anywhere yet – Stage 4a is diagnostic-only – but kept on the result rather than discarded, since a future precision-improvement stage would need exactly this.\n\n\n\n\n\n","title":"HomotopyGetsReal.ResolveResult"},{"category":"function","location":"api/#HomotopyGetsReal.resolve_isosingular_dimension","page":"API","text":"resolve_isosingular_dimension(F_original::System, x0::AbstractVector, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> ResolveResult{T}\n\nIsosingular-deflation Stage 4a primitive (2026-07): the orchestrator Stage 3 deliberately didn't build – ties estimate_corank, _corank_plateau_hint, deflate_once, and verify_isosingular_dimension together into a single call that resolves a point's isosingular local dimension (Hauenstein-Wampler Definition 5.18) from a corank sequence's first entry through to a verified terminal round.\n\nLoop, once per round: if the current corank is 0, or _corank_plateau_hint says the sequence might have stabilized, call verify_isosingular_dimension on the CURRENT (possibly already-deflated) system against the ORIGINAL F_original – Verified returns Resolved immediately; Inconclusive returns Ambiguous immediately, not retried at this level. Otherwise (_corank_plateau_hint says \"definitely not yet\", or verification returned NotTerminal), call deflate_once and continue, up to cfg.max_deflations rounds before returning Exhausted.\n\nA genuine nonzero-limit plateau always costs one deflation round beyond where the corank first reaches its true limit (_corank_plateau_hint needs to see a repeated value to recognize a plateau) – this cost does not appear for a d==0 resolution, which always triggers verification on the SAME round it's first observed. cfg.max_deflations = 10 is, on the identical footing as cfg.isosingular_verify_retries, an explicit safety margin for cases this project hasn't exercised yet – not a value any ground-truth case has needed more than a fifth of. See docs/DESIGN_NOTES.md §isosingular-deflation, Stage 4a, for the measured round-count table across every ground-truth case and later Stage 4c validation data.\n\nrng: threaded straight through to the internal verify_isosingular_dimension call, so a caller who needs a reproducible verdict/rounds on a case whose plateau lands on a nonzero corank can pass a seeded rng for a deterministic result.\n\nTest instrumentation (2026-07): _resolve_isosingular_trace, a private Base.ScopedValue, is checked at each of this function's 3 return points and, when set to a Vector (via Base.ScopedValues.with(_resolve_isosingular_trace => log) do ... end around a caller several frames up – decompose_3d_surface's own compute_critical_points/intersect_bounding_object call sites never pass anything explicitly, so this is invisible to them), has a (verdict, dim, rounds, attempts, corank_seq) NamedTuple pushed onto it before returning. This exists so test/test_isosingular_deflation.jl can observe every real firing (including ones absorbed during clustering before reaching a final decomposition's output) without monkeypatching this function – see that test file's own comment for why a same-name shadow copy would never be reached, and why permanently redefining this function in place (the prior approach) could silently drift from whatever this docstring's logic actually does. Default nothing: a single pointer-equality check, zero-cost for the one production caller path (compute_critical_points), which never sets this.\n\n\n\n\n\n","title":"HomotopyGetsReal.resolve_isosingular_dimension"},{"category":"function","location":"api/#HomotopyGetsReal.is_near_singular","page":"API","text":"is_near_singular(F::System, point::Vector{ComplexF64}, expected_rank::Int, rank_tol::Float64, sv_thresh::Float64)\n    -> Bool\n\nReturn whether point is numerically singular for adaptive path tracking.\n\nUsed during Float64 tracking in track_path to trigger step bisection. point must match F.variables in length and order. Returns true when the Jacobian rank falls below expected_rank or the smallest singular value is below sv_thresh.\n\n\n\n\n\n","title":"HomotopyGetsReal.is_near_singular"},{"category":"function","location":"api/#HomotopyGetsReal.build_tracker","page":"API","text":"build_tracker(H_sys::System, x_start::Float64, cfg::HomotopyConfig{T}; compile::Symbol = :all) where {T<:AbstractFloat}\n    -> (ph::ParameterHomotopy, tracker::Tracker)\n\nBuild a ParameterHomotopy and Tracker for track_path and track_bidirectional.\n\nInitialize both parameter slots to x_start; update them with start_parameters! and target_parameters! on each step. Pass compile = :none when constructing many short-lived systems (e.g. per-anchor face tracking); keep the default :all when reusing one system. cfg.path_tracker_precision sets TrackerOptions(min_step_size).\n\n\n\n\n\n","title":"HomotopyGetsReal.build_tracker"},{"category":"function","location":"api/#HomotopyGetsReal.track_path","page":"API","text":"track_path(\n    F::System, ph::ParameterHomotopy, tracker::Tracker,\n    y_start::Vector{ComplexF64}, x_start::Float64, x_target::Float64,\n    max_steps::Int, rank_tol::Float64, sv_thresh::Float64, poor_acc_tol::Float64, min_width::Float64,\n) -> (y_final::Vector{ComplexF64}, path::Vector{Vector{Float64}})\n\nTrack one parameter-homotopy step from x_start to x_target with adaptive bisection.\n\nF is the original defining system (for singularity checks only); ph/tracker carry the parameterized system built by the caller. Returns the landing y at x_target and sampled [x, real.(y)...] points along the way.\n\n\n\n\n\n","title":"HomotopyGetsReal.track_path"},{"category":"function","location":"api/#HomotopyGetsReal.track_bidirectional","page":"API","text":"track_bidirectional(\n    F::System, ph::ParameterHomotopy, tracker::Tracker,\n    y_mid::Vector{ComplexF64}, x_mid::Float64, x_left::Float64, x_right::Float64,\n    max_steps::Int, rank_tol::Float64, sv_thresh::Float64, poor_acc_tol::Float64, min_width::Float64,\n) -> (full_path::Vector{Vector{Float64}}, y_land_left::Vector{ComplexF64}, y_land_right::Vector{ComplexF64})\n\nTrack from a midslice point toward both interval endpoints and assemble the full path.\n\nCalls track_path toward x_left and x_right independently. Returns reverse(path_left) ++ midpoint ++ path_right plus both landing values for endpoint matching.\n\n\n\n\n\n","title":"HomotopyGetsReal.track_bidirectional"},{"category":"function","location":"api/#HomotopyGetsReal.compute_midslice","page":"API","text":"compute_midslice(F::System, x_left::T, x_right::T, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> Vector{Complex{T}}\n\nFind real y-roots of a plane curve at the midpoint between two x-bounds.\n\nF must be a raw plane curve (one equation, two variables). Fixes the first variable at x_mid = (x_left + x_right) / 2 and returns nearly-real roots within cfg.critical_point_tol. Call only between adjacent distinct x-slots, not at a vertex x-coordinate.\n\n\n\n\n\n","title":"HomotopyGetsReal.compute_midslice"},{"category":"function","location":"api/#HomotopyGetsReal.connect_the_dots!","page":"API","text":"connect_the_dots!(\n    F::System,\n    x_left::T, x_mid::T, x_right::T,\n    y_mid::Complex{T},\n    edge_id::Int,\n    vertices::Vector{NativeVertex{T}},\n    cfg::HomotopyConfig{T},\n) where {T<:AbstractFloat}\n    -> Edge{T}\n\nConnect one curve branch between two x-bounds by bidirectional path tracking.\n\nCall once per y-root from compute_midslice. Tracks from (x_mid, y_mid) toward both endpoints, resolves landings against vertices (mutating it when needed), and returns an Edge with raw sampled points. is_singular is true when either endpoint is Singular.\n\n\n\n\n\n","title":"HomotopyGetsReal.connect_the_dots!"},{"category":"function","location":"api/#HomotopyGetsReal.sample_edge","page":"API","text":"sample_edge(F::System, edge::Edge{T}, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> Edge{T}\n\nResample an edge to cfg.edge_sample_density equidistant points along arc length.\n\nNo longer \"pure geometry\" (2026-07 fix, see docs/DESIGN_NOTES.md's Watertightness measurements entry for the investigation). The arc-length redistribution below is still plain linear interpolation between the raw tracked points in edge.sampled_points – but for a curve segment smooth enough that PathTracking._track_path_segment! never needed to bisect (e.g. a plain circle, or any well-conditioned arc), that raw path can be as sparse as 2 points, and interpolating cfg.edge_sample_density points along the straight chord between them lands every interior point measurably OFF the true curve – confirmed directly on this project's own Taubin heart fixture (residuals up to 0.28, orders of magnitude above cfg.critical_point_tol), not just a synthetic worst case. Every interpolated point is now corrected back onto f(x, y) = 0 via _project_to_curve (the same Gauss-Newton re-projection FaceTracking._project_to_slice already uses for the analogous 3D case) before being returned. F is therefore now a required argument. Ids, left_vertex_id/right_vertex_id, and the singularity flag are unchanged; degenerate (single-point) edges are still repeated, now through the same projection for consistency.\n\n\n\n\n\n","title":"HomotopyGetsReal.sample_edge"},{"category":"function","location":"api/#HomotopyGetsReal.decompose_1d_curve","page":"API","text":"decompose_1d_curve(F::System, cfg::HomotopyConfig{T}; deflate::Bool = false) where {T<:AbstractFloat}\n    -> (vertices::Vector{NativeVertex{T}}, edges::Vector{Edge{T}})\n\nDecompose a raw plane curve into vertices and resampled edges.\n\nFinds critical and boundary vertices, merges coincident ones, connects adjacent x-intervals via midslice witnesses, and returns equidistantly sampled edges.\n\nIsosingular deflation, Stage 4b (2026-07), diagnostic-only: deflate = true passes deflate = true, F_original = F through to both compute_critical_points (called here on F_aug, the y-critical-augmented system, NOT the bare curve) and intersect_bounding_object (called here on F directly, already the bare curve) – F itself, this function's own raw argument, is exactly the F_original both need, and is already in scope here regardless. false (the default) is the exact pre-Stage-4 code path.\n\n\n\n\n\n","title":"HomotopyGetsReal.decompose_1d_curve"},{"category":"function","location":"api/#HomotopyGetsReal.build_patch_system","page":"API","text":"build_patch_system(F::System)\n\nPrepare symbolic data for face tracking from a raw surface system.\n\nF must be a single implicit surface with variables ordered [x, y, z] (z last), matching compute_critical_z_slices. Differentiates f once and builds F_for_tracking with variables reordered to [z, x, y] so z can be swept as a homotopy parameter.\n\nReturns\n\nA NamedTuple with fields f, fx, fy, fz, x_var, y_var, z_var, and F_for_tracking.\n\n\n\n\n\n","title":"HomotopyGetsReal.build_patch_system"},{"category":"function","location":"api/#HomotopyGetsReal.patch_direction","page":"API","text":"patch_direction(patch, x0, y0, z0, cfg)\n\nReturn patch line coefficients (a, b) at anchor (x0, y0, z0).\n\nThe patch equation is a*(x - x0) + b*(y - y0) = 0, with (a, b) = (f_y, -f_x) from the surface gradient at the anchor.\n\n\n\n\n\n","title":"HomotopyGetsReal.patch_direction"},{"category":"function","location":"api/#HomotopyGetsReal.build_face_tracker","page":"API","text":"build_face_tracker(patch, x0, y0, z0, cfg)\n\nBuild a per-anchor homotopy tracker for sweeping a slice point in z.\n\nConstructs a two-equation system from the surface and a literal patch line at (x0, y0, z0), with only z as the homotopy parameter.\n\nReturns\n\n(H_sys, ph, tracker) ready for track_dense_path.\n\n\n\n\n\n","title":"HomotopyGetsReal.build_face_tracker"},{"category":"function","location":"api/#HomotopyGetsReal.track_dense_path","page":"API","text":"track_dense_path(F, ph, tracker, y_start, param_start, param_targets, ...)\n\nTrack a path through a sequence of parameter values, keeping one point per target.\n\nWalks param_targets in order, calling _track_path_segment! between consecutive targets with adaptive bisection on poor quality or near-singular steps.\n\nF should be patch.F_for_tracking (the raw surface Jacobian system), not the per-anchor augmented system.\n\nReturns\n\n(y_final, dense_path) where dense_path has exactly length(param_targets) entries, each [param, x, y].\n\n\n\n\n\n","title":"HomotopyGetsReal.track_dense_path"},{"category":"function","location":"api/#HomotopyGetsReal.track_face","page":"API","text":"track_face(F, patch, edge, z_mid, z_bottom, z_top, face_id, cfg)\n\nSweep an edge across z and assemble one Face mesh.\n\nRe-projects each column anchor onto the slice at z_mid, sweeps it bidirectionally to [z_bottom, z_top], and triangulates the resulting grid.\n\nGrid layout\n\nColumns follow edge.sampled_points (curve arclength order).\nRows run from z_bottom to z_top through the exact anchor at z_mid.\nStorage is row-major: point (r, c) at mesh_vertices[(r - 1) * n_curve + c, :].\n\nWinding normalization happens later in weld_mesh, not here.\n\nReturns\n\nA Face{T} with mesh_vertices and mesh_topology.\n\n\n\n\n\n","title":"HomotopyGetsReal.track_face"},{"category":"function","location":"api/#HomotopyGetsReal.sweep_face_bidirectional","page":"API","text":"sweep_face_bidirectional(F, patch, x0, y0, z_mid, z_bottom, z_top, cfg)\n\nBidirectionally sweep one anchor from z_mid toward z_bottom and z_top.\n\nUses cfg.midslice_sample_density intermediate targets per direction, excluding the shared start at z_mid and including the endpoint. Each direction builds its own tracker and may re-anchor during the sweep.\n\nReturns\n\n(dense_path_down, dense_path_up), each a vector of [z, x, y] points.\n\n\n\n\n\n","title":"HomotopyGetsReal.sweep_face_bidirectional"},{"category":"function","location":"api/#HomotopyGetsReal.random_orthogonal_matrix","page":"API","text":"random_orthogonal_matrix(::Type{T}, n::Int; rng = Random.default_rng()) where {T<:AbstractFloat}\n    -> Matrix{T}\n\nDraw a Haar-uniform random rotation matrix in SO(n).\n\nConstructed by QR decomposition of a Gaussian random matrix with the sign correction that makes the distribution Haar-uniform on O(n), then mapped onto SO(n). Distributionally equivalent to the Stewart Householder-reflection construction BertiniReal/Bertini1 use (make_matrix_random_real_mp). Pass a seeded rng for reproducibility.\n\n\n\n\n\n","title":"HomotopyGetsReal.random_orthogonal_matrix"},{"category":"function","location":"api/#HomotopyGetsReal.compute_critical_z_slices","page":"API","text":"compute_critical_z_slices(F::System, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> Vector{T}\n\nFind critical z-values where the surface cannot be written as z = g(x, y).\n\nF must be one equation in three variables ordered [x, y, z] (z last). Delegates to compute_critical_points and clusters the resulting z-coordinates with cfg.vertex_match_tol.\n\nArguments\n\nF::System: surface system.\ncfg::HomotopyConfig{T}: tolerances for solving and clustering.\n\nReturns\n\nSorted Vector{T} of distinct z-values.\n\n\n\n\n\n","title":"HomotopyGetsReal.compute_critical_z_slices"},{"category":"function","location":"api/#HomotopyGetsReal.slice_at_z","page":"API","text":"slice_at_z(F::System, z_val::T, cfg::HomotopyConfig{T}; deflate::Bool = false) where {T<:AbstractFloat}\n    -> (vertices_3d::Vector{NativeVertex{T}}, edges_3d::Vector{Edge{T}})\n\nDecompose the plane curve f(x, y, z_val) = 0 and lift it to 3D at fixed z.\n\nSubstitutes z => z_val, runs decompose_1d_curve on the resulting 2-variable system, and appends z_val as the third coordinate of every vertex and edge sample. Vertex and edge ids are local to this call; renumber before combining slices (as decompose_3d_surface does).\n\nIsosingular deflation, Stage 4c (2026-07): deflate passes straight through to decompose_1d_curve(F_2d, cfg; deflate = deflate) – F_2d (this function's own per-slice 2-variable substitution, built below) is automatically what decompose_1d_curve uses as its F_original, per its own existing Stage 4b wiring; no separate F_original argument is needed or threaded here. This is deliberate, not an oversight: a vertex flagged Singular here was flagged because the 2D SLICE curve f(x,y,z_val)=0 has a rank-deficient Jacobian – deflating against the 3-variable surface F instead would ask a different, higher-dimensional question (see _surface_critical_vertices, which asks exactly that question, separately and deliberately).\n\nArguments\n\nF::System: surface system (length(F.expressions) == 1, nvariables(F) == 3).\nz_val::T: slice height.\ncfg::HomotopyConfig{T}: curve decomposition settings.\n\nReturns\n\nA tuple of 3D vertices and edges at the given z.\n\n\n\n\n\n","title":"HomotopyGetsReal.slice_at_z"},{"category":"function","location":"api/#HomotopyGetsReal.decompose_3d_surface","page":"API","text":"decompose_3d_surface(F::System, cfg::HomotopyConfig{T}; projection = nothing, rng = Random.default_rng(),\n                       incidence = false, deflate = false) where {T<:AbstractFloat}\n    -> (vertices, edges, faces, mesh)\n\nDecompose a real algebraic surface into vertices, edges, faces, and a welded mesh.\n\nF must be a single equation in three variables ordered [x, y, z] (z last). The pipeline finds critical z-slices inside cfg.bbox_z, decomposes a mid-z curve in each slab, sweeps faces between slab bounds, and welds everything into one GeometryBasics.Mesh. This is the 3D analogue of decompose_1d_curve.\n\nArguments\n\nF::System: surface system (length(F.expressions) == 1, nvariables(F) == 3).\ncfg::HomotopyConfig{T}: tolerances, bounding box, and sampling densities.\n\nKeyword arguments (Phase 8)\n\nprojection: nothing (default) sweeps along the literal z axis with the exact pre-Phase-8 behavior – no coordinate transform is applied at all. :random draws a Haar-uniform SO(3) rotation internally (seed via rng); a user-supplied 3x3 orthonormal, det = +1 matrix Q is validated and used directly. When a projection is active the pipeline runs on the chart system F'(x') = F(Q x') over the enclosing axis-aligned box of the rotated world bbox (slightly larger than the world box at the same cfg); _verify_projection_ok rejects projections that leave an augmenting partial identically zero (the z - x^2 crash class) with a loud ArgumentError; and every returned vertex coordinate, edge sample, face mesh row, and welded mesh point is mapped back to world coordinates via p = Q p'. Face.mid_slice_z and boundary-vertex :fixed_variable/:fixed_value metadata remain CHART-frame quantities. Callers who need Q itself should construct it with random_orthogonal_matrix and pass the matrix explicitly – :random is convenience for exploratory use.\nrng: random source used only by projection = :random.\nincidence (Phase 9a/9b): false (default) keeps the exact 4-tuple return and a byte-identical code path (no crit-slice decompositions, no extra solves, weld_mesh runs its original tolerance-only welding). true additionally decomposes every interior slab boundary an actual face touches (the crit-slices), assigns each face's boundary columns to crit-slice cells (nearest-wins, distances recorded), checks branch continuity between consecutive columns (flagged, not erred — see SurfaceIncidence.continuity_ok), and passes the result into weld_mesh, which additionally SNAPS confident landings onto shared crit-slice targets before clustering, lofts full-coverage ribbon triangulations against confident :edge-kind runs, and fixes any resulting T-junctions afterward (see weld_mesh's own Phase 9b/9c section) — closing the fold/point-type boundaries completely and substantially reducing (not fully closing) the multi-face edge-type boundaries; see docs/DESIGN_NOTES.md, \"Surface decomposition, Phases 8-9\" / \"Watertightness measurements\", for the measured closure progression and cost. Returns a 5-tuple whose last element is a SurfaceIncidence. Composes with projection (incidence and stitching happen in the chart, then world-map).\ndeflate (Stage 4c, 2026-07): false (default) is the exact pre-Stage-4 code path – confirmed byte-identical, not assumed (existing sphere/ ellipsoid fixtures pass unmodified with this keyword never mentioned). true threads deflate = true down through _robust_slice_at_z/ _decompose_crit_slice/slice_at_z/decompose_1d_curve (every Singular vertex from a z-slice gets diagnosed against that slice's own 2-variable curve, F_original = F_2d) and, when incidence = true also, through _surface_critical_vertices (every 3D fold-anchor critical point gets diagnosed against the full 3-variable F directly) – see slice_at_z's and _surface_critical_vertices's own docstrings for why these are two deliberately DIFFERENT questions, not the same one asked twice, and can legitimately disagree on the same physical point. Diagnostic-only: metadata[:isosingular_verdict]/:isosingular_dimension/ :isosingular_deflation_sequence are added to Singular vertices; nothing about coordinates, v_type, edges, faces, or the mesh changes.\n\nKnown limitation: generic projections over singular curves\n\nWhen a projection makes a positive-dimensional singular curve of the surface (∇f = 0 along a curve, e.g. the Taubin heart's z=0 ellipse) transversal to the slicing planes, mesh quality degrades in a narrow band around that curve; away from the singular locus the decomposition is unaffected. See docs/DESIGN_NOTES.md, \"Surface decomposition, Phases 8-9\" / \"Known limitation: generic projections over singular curves\", for the measured extent of this band. deflate = true (Stage 4c, 2026-07) diagnoses individual Singular vertices' isosingular local dimension but does not itself change mesh quality in this band – it is diagnostic-only (see deflate's own entry above); actually USING that diagnosis to improve mesh quality here (e.g. replacing a vertex's coordinates with a deflation-verified endpoint) remains future work, narrower in scope than \"decomposing singular curves\" was originally stated as. See also _robust_slice_at_z's Phase 8 amendments section.\n\nReturns\n\nA 4-tuple (vertices, edges, faces, mesh) of types Vector{NativeVertex{T}}, Vector{Edge{T}}, Vector{Face{T}}, and GeometryBasics.Mesh, in world coordinates.\n\n\n\n\n\n","title":"HomotopyGetsReal.decompose_3d_surface"},{"category":"function","location":"api/#HomotopyGetsReal.weld_mesh","page":"API","text":"weld_mesh(faces::Vector{Face{T}}, patch::NamedTuple, cfg::HomotopyConfig{T}; incidence = nothing) where {T<:AbstractFloat}\n    -> GeometryBasics.Mesh\n\nMerge per-face meshes into one watertight GeometryBasics.Mesh.\n\nCollects every face's mesh_vertices and mesh_topology, clusters coincident vertices across face boundaries with cfg.vertex_match_tol, remaps triangle indices, drops degenerate triangles, and flips winding so normals align with ∇f.\n\nArguments\n\nfaces::Vector{Face{T}}: swept faces with local mesh data.\npatch::NamedTuple: surface patch system (for gradient-based winding).\ncfg::HomotopyConfig{T}: vertex-matching tolerance.\n\nKeyword arguments (Phase 9b/9c)\n\nincidence: nothing (default) is the exact pre-Phase-9b behavior – tolerance-only welding, byte-identical code path (no _snap_boundary_points! call, no loft, no _split_t_junctions). A SurfaceIncidence (from decompose_3d_surface(...; incidence = true)) activates three layered mechanisms, in order:\n_snap_boundary_points! (Phase 9b): snaps confident :crit_slice_vertex/:critical_point landings directly onto their cell's own coordinates – this ALONE fully closes fold/point-type boundaries (cusp, lobe tips, sphere/ellipsoid poles), since every column converges to the SAME single point regardless of which face touches it. Its own :edge-kind pooled-snap pass also still runs, but its effect on any column covered by step 2 below is superseded (harmless, unreferenced once that column's boundary-row triangles are replaced).\n_identify_edge_runs + _append_loft_triangles! (Phase 9c): for :edge-kind confident column runs, REPLACES the boundary row's own quad triangulation with a full-coverage ribbon (_bridge_ribbon) against the crit-slice edge's COMPLETE sample sequence (every point, not a snap-derived subset) plus seam-capping triangles at each run's un-lofted ends.\n_chained_edge_polylines + _split_t_junctions: fixes resulting T-junctions, INCLUDING junction points shared between adjacent crit-slice edges (chained into one polyline first), not just within a single edge's own polyline.\nThe fold/point-type boundaries are already fully closed by step 1 alone and are unaffected by steps 2-3, which close a further share of the remaining multi-edge, multi-face EDGE-type boundaries (the singular notch, the saddle pair) without fully closing them. Full closure of those is DEFERRED as a future, separately-scoped phase, same treatment as full singular-curve decomposition. See docs/DESIGN_NOTES.md, \"Surface decomposition, Phases 8-9\" / \"Watertightness measurements\" and \"Phase 9c: edge chaining, loft triangulation, and the Option A investigation\", for the measured closure progression and the higher-fidelity alternative that was evaluated and rejected.\n\nReturns\n\nA welded GeometryBasics.Mesh with Point3f vertices.\n\n\n\n\n\n","title":"HomotopyGetsReal.weld_mesh"},{"category":"type","location":"api/#HomotopyGetsReal.CritSlice","page":"API","text":"CritSlice{T<:AbstractFloat}\n\nPhase 9a: the 1D decomposition of the surface AT one interior slab boundary (a critical z-value), as opposed to the mid-slab slices the pipeline sweeps from. Computed by _decompose_crit_slice when decompose_3d_surface runs with incidence = true.\n\nFields\n\nboundary_index: j such that this slice sits at z_bounds[j] (interior boundaries only; bbox endpoints never get a CritSlice).\nz: the critical z-value (CHART frame under a projection, like Face.mid_slice_z).\nvertices, edges: the slice's cells, id-renumbered into the same global namespace as the main decomposition's cells but NOT appended to the main return (backward compatibility; ids never collide).\nis_degenerate: true iff the slice has no edges — a fold/point-type boundary (measured: sphere/ellipsoid extremes decompose to EMPTY slices, Taubin cusp/tips to isolated Singular vertices; nodal saddle-type boundaries decompose to real curves). Fold-type boundaries are anchored to the surface's 3D critical vertices instead (see _assign_landings).\n\n\n\n\n\n","title":"HomotopyGetsReal.CritSlice"},{"category":"type","location":"api/#HomotopyGetsReal.ColumnLanding","page":"API","text":"ColumnLanding{T<:AbstractFloat}\n\nPhase 9a: the incidence assignment of ONE swept column's landing point at one slab boundary. kind is :edge (a CritSlice edge), :crit_slice_vertex, :critical_point (a 3D surface critical vertex — the fold-type anchor), :bbox (the boundary is a bounding-box endpoint, no crit-slice exists), or :none (no candidate cell found); id is the cell id in the matching namespace (0 for :bbox/:none); dist is the Euclidean distance from the landing point to the assigned cell.\n\nDistances are RECORDED, never thresholded, in Phase 9a: assignment is nearest-wins, and the printed distance distributions (see test/test_taubin.jl's 9a section) are the evidence base from which Phase 9b will derive real acceptance thresholds — deliberately not guessed ahead of the data (the same discipline the Phase 8 gate history taught).\n\n\n\n\n\n","title":"HomotopyGetsReal.ColumnLanding"},{"category":"type","location":"api/#HomotopyGetsReal.SurfaceIncidence","page":"API","text":"SurfaceIncidence{T<:AbstractFloat}\n\nPhase 9a: face/edge incidence data for a surface decomposition — which crit-slice cells each face's swept columns land on at its slab boundaries. Returned as the 5th element of decompose_3d_surface when incidence = true.\n\nFields\n\ncrit_slices: one CritSlice per interior slab boundary that an actual face touches (lazily computed; sorted by boundary_index).\nface_bottom_edges / face_top_edges: face id → ordered unique crit-slice edge ids its boundary columns land on.\nface_bottom_anchor / face_top_anchor: face id → 3D critical-vertex id for fold-type boundaries (0 when none).\nedge_faces: inverse adjacency, crit-slice edge id → face ids touching it.\ncolumn_landings_bottom / column_landings_top: face id → per-column ColumnLanding in curve order.\ncritical_vertices (Phase 9b): the surface's 3D critical points (fold-type landing anchors), stored so :critical_point-kind landing ids resolve to coordinates — needed for snap-unification, not stored in Phase 9a since nothing there looked them up by id after assignment.\ncontinuity_ok (Phase 9b): face id → true iff every pair of CONFIDENT (see _landing_confidence) consecutive columns, on both sides, lands on combinatorially adjacent crit-slice cells (see _cells_adjacent). Absent keys mean the face touched no crit-slice (both sides :bbox/empty). Currently a write-only diagnostic field: populated by _check_continuity! during decompose_3d_surface, but nothing in weld_mesh or elsewhere in the pipeline reads it — it does not affect any mesh actually produced today. It exists to gather firing-rate evidence ahead of a possible future promotion to an enforced check (see below).\ncontinuity_violations_bottom / continuity_violations_top (Phase 9b): face id → indices c of violating consecutive pairs (c, c+1).\n\nPromoting continuity_ok from a flag to a hard error is a deliberate FUTURE decision contingent on the firing-rate evidence this structure gathers on real fixtures — not something decided now, and not safe to decide until the flags themselves are trustworthy.\n\n_cells_adjacent treats a :critical_point landing coincident (within cfg.vertex_match_tol) with a :crit_slice_vertex/edge-endpoint landing as adjacent — these represent the SAME physical fold-tip location by construction, not independent cells. See docs/DESIGN_NOTES.md, \"Surface decomposition, Phases 8-9\" / \"Phase 9a/9b: crit-slice incidence and the critical_point coincidence fix\", for the diagnostic investigation that found and fixed the false-positive continuity_ok firing rate this rule resolves.\n\n\n\n\n\n","title":"HomotopyGetsReal.SurfaceIncidence"},{"category":"function","location":"api/#HomotopyGetsReal.plot_curve_decomposition","page":"API","text":"plot_curve_decomposition(vertices, edges; kwargs...) -> Figure\n\nPlot a 2D curve decomposition.\n\nDraws resampled edges as polylines and, optionally, vertices colored and marked by VertexType.\n\nKeyword arguments\n\ncfg: if given, set axis limits from cfg.bbox_x and cfg.bbox_y.\nshow_vertices: scatter vertices by type (default true).\nshow_labels: annotate vertex ids (default false).\nedge_color_by: :cell gives each edge a distinct color; :mono uses edge_color.\nedge_color, colormap: styling for mono or per-edge coloring.\n\nReturns a Figure without calling display.\n\n\n\n\n\n","title":"HomotopyGetsReal.plot_curve_decomposition"},{"category":"function","location":"api/#HomotopyGetsReal.plot_surface_decomposition","page":"API","text":"plot_surface_decomposition(mesh; kwargs...) -> Figure\n\nPlot a welded 3D surface mesh.\n\nPreferred method for output from decompose_3d_surface.\n\nKeyword arguments\n\ncolor_by: :x, :y, :z, or f(x, y, z) -> Real for vertex colors.\ncolormap: color map for the mesh (default :viridis).\nshow_wireframe: overlay triangle edges (default false).\nshow_colorbar: show a color bar (default true).\nvertices: optional vertex overlay, colored by VertexType.\ncfg: if given, set axis limits from bounding box fields.\n\nReturns a Figure without calling display.\n\n\n\n\n\nplot_surface_decomposition(faces; kwargs...) -> Figure\n\nPlot individual face meshes with one color per face.\n\nUseful for inspecting per-cell topology before welding. Does not apply weld_mesh's global winding correction; some faces may look inverted under directional lighting.\n\nKeyword arguments\n\ncolormap: categorical colors cycled over faces.\nshow_wireframe: overlay triangle edges (default false).\ncfg: if given, set axis limits from bounding box fields.\n\nEmits a one-time warning about winding on first call.\n\n\n\n\n\n","title":"HomotopyGetsReal.plot_surface_decomposition"},{"category":"function","location":"api/#HomotopyGetsReal.interactive_3d_viewer","page":"API","text":"interactive_3d_viewer(mesh; kwargs...) -> Figure\n\nOpen an interactive 3D viewer for a surface mesh.\n\nActivates GLMakie, builds the figure with plot_surface_decomposition, and displays it. Keyword arguments match that method.\n\n\n\n\n\n","title":"HomotopyGetsReal.interactive_3d_viewer"},{"category":"section","location":"#HomotopyGetsReal","page":"Home","text":"using Markdown\nreadme = read(joinpath(@__DIR__, \"..\", \"..\", \"README.md\"), String)\n# Drop the leading H1 so Documenter's page title is not duplicated.\nreadme = replace(readme, r\"^# HomotopyGetsReal\\r?\\n+\" => \"\"; count = 1)\nMarkdown.parse(readme)","title":"HomotopyGetsReal"},{"category":"section","location":"#Documentation-pages","page":"Home","text":"Pages = [\"api.md\"]\nDepth = 2","title":"Documentation pages"}]
}
