var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API","text":"Exported symbols from HomotopyGetsReal. Docstrings are taken from the package sources (Phase 7 docstring audit); no additional narrative is defined here.","title":"API reference"},{"category":"section","location":"api/#Configuration-and-types","page":"API","text":"","title":"Configuration and types"},{"category":"section","location":"api/#Clustering","page":"API","text":"","title":"Clustering"},{"category":"section","location":"api/#Critical-points-and-bounding-objects","page":"API","text":"","title":"Critical points and bounding objects"},{"category":"section","location":"api/#Path-tracking","page":"API","text":"","title":"Path tracking"},{"category":"section","location":"api/#Curve-topology","page":"API","text":"","title":"Curve topology"},{"category":"section","location":"api/#Face-tracking","page":"API","text":"","title":"Face tracking"},{"category":"section","location":"api/#Surface-decomposition","page":"API","text":"","title":"Surface decomposition"},{"category":"section","location":"api/#Visualization","page":"API","text":"","title":"Visualization"},{"category":"type","location":"api/#HomotopyGetsReal.HomotopyConfig","page":"API","text":"HomotopyConfig{T<:AbstractFloat}\n\nCentral configuration object for the HomotopyGetsReal pipeline. All tolerances and numerical knobs used across the six algorithmic steps live here so they can be tuned in one place, and so the entire pipeline can be instantiated in a different floating-point precision T (e.g. Float64 for speed, BigFloat for certified/high-precision runs) simply by writing HomotopyConfig{BigFloat}().\n\nAll numeric defaults are wrapped in T(...) so that constructing HomotopyConfig{BigFloat}() actually gets BigFloat-precision defaults instead of Float64 literals silently truncating the precision.\n\nFields\n\nTolerances (the three easy-to-confuse ones)\n\nThese three tolerances all bound \"how close is close enough\", but they are used at very different stages of the pipeline and mixing them up is a classic source of subtle bugs:\n\ncritical_point_tol::T: Used during compute_critical_points. Bounds how close the path tracker's endpoint must be to an actual solution of the critical-point system (i.e. residual/Jacobian-based stopping tolerance for Newton correction) before a numerically-found point is accepted as a genuine critical point of the algebraic variety. This is about solution quality of a single homotopy path.\nboundary_point_tol::T: Used during intersect_bounding_object. Bounds how close a tracked point must be to the bounding box's faces (or other bounding object) before it is classified as lying on the boundary rather than strictly inside/outside it. This is about geometric containment, not path-tracking residual.\nvertex_match_tol::T: Used during ConnectTheDots! / Merge / GetMergeCandidates. Bounds how close two independently-computed NativeVertex coordinates must be to be considered the same geometric vertex (e.g. a critical point recomputed from two adjacent slices, or a vertex found via two different homotopy paths). This is about deduplication/identity, not solution quality or containment.\n\nIn short: critical_point_tol asks \"did the solver converge?\", boundary_point_tol asks \"is this point on the box?\", and vertex_match_tol asks \"are these two points actually the same vertex?\".\n\nOther tolerances\n\njacobian_rank_tol::T: Threshold below which a singular value of the Jacobian is treated as zero when computing its numerical rank (used to detect singular points / rank-deficient loci).\nsingular_value_threshold::T: Threshold used when classifying a vertex as Singular from the smallest singular value(s) of the Jacobian at that point (distinct from jacobian_rank_tol, which is used purely for rank computation; this one drives vertex classification/metadata).\npath_tracker_precision::T: Requested precision passed to HomotopyContinuation.jl's path tracker (controls adaptive step-size/Newton corrector precision along a homotopy path).\npatch_transversality_cos_tol::T: Used by FaceTracking._sweep_direction's adaptive re-anchoring. Bounds the cosine of the angle between a z-sweep patch anchor's ORIGINAL fixed gradient direction and the CURRENT local surface gradient (both restricted to their in-slice (x,y) components) before the patch is considered too skewed relative to the curve's drifting tangent and is rebuilt at the current point. This is a genuinely different kind of quantity from jacobian_rank_tol/singular_value_threshold above (which bound absolute Jacobian singular values of the system actually being solved, at whatever scale that system's own equations happen to carry) – this one is a dimensionless, scale-free RATIO (a cosine), because the quantity of interest here is purely the rotation of the gradient direction, not any solve residual or absolute singular value. Reusing singular_value_threshold for this was tried first and found empirically unusable: on an asymmetric-ellipsoid regression case, the augmented system's smallest singular value stayed well above singular_value_threshold's default (1e-6) for every sample point right up until the hop where transversality had already collapsed and the tracked point had already landed measurably off-surface (the singular value's absolute scale is set by the surface's own gradient magnitude at that point, not by how close the patch is to becoming tangent to the curve) – i.e. that check reliably fired one hop too late to actually prevent the failure it was meant to catch. The cosine threshold instead directly measures the geometric quantity that determines patch validity (see _sweep_direction's docstring for the full derivation of why H_sys's Jacobian determinant is exactly this dot product), so a single default (0.9, i.e. re-anchor once the gradient has rotated more than ~26° from the anchor) is meaningful independent of the surface's scale or midslice_sample_density.\n\nNumerical/algorithmic knobs\n\nmax_path_steps::Int: Maximum number of steps the path tracker is allowed to take before a path is declared a failure/truncation.\nbbox_x::Tuple{T,T}, bbox_y::Tuple{T,T}, bbox_z::Tuple{T,T}: The (min, max) extents of the bounding box used by intersect_bounding_object along each axis.\nedge_sample_density::Int: Number of points sampled along each Edge in sample_edge.\nmidslice_sample_density::Int: Number of sample points used when building a mid-slice mesh in interslice/MidSlice!.\nz_mid_retry_frac::T: Used by SurfaceDecomposition._robust_slice_at_z. When a slab's naive z_mid = (z_bottom+z_top)/2 produces a Topology.decompose_1d_curve result containing BOTH an Artificial vertex tagged metadata[:origin] == :endpoint_fallback (i.e. Topology._resolve_endpoint had to fabricate a vertex because a tracked path failed to close back onto any known vertex) AND a Singular-typed vertex (the co-occurrence, not :endpoint_fallback alone, is what actually distinguishes a genuinely non-reduced/ repeated-factor curve – e.g. the Taubin heart surface's z_mid=0 slice – from a merely topologically-complex-but-well-conditioned one; see _robust_slice_at_z's own docstring for the empirical comparison that ruled out both \":endpoint_fallback alone\" and a raw residual-magnitude threshold), _robust_slice_at_z retries at a perturbed z_mid. This field is the perturbation step size, expressed as a FRACTION OF THE SLAB WIDTH (z_top - z_bottom) rather than an absolute offset, so the same default behaves sensibly regardless of a given surface's own z-extent. A new field, not a reuse of vertex_match_tol/critical_point_tol/etc.: none of those tolerances mean \"how far to nudge a slicing plane\", and reusing one anyway would tie an unrelated numerical knob's tuning to this different job (the same reasoning that justified patch_transversality_cos_tol as its own field above). Default 0.01 (1% of slab width) is empirically generous: on the Taubin heart's degenerate z_mid=0 case, even a 0.4%-of-width perturbation already produced a clean, non-Artificial decomposition (scratch_phase5_taubin_check.jl section 6's retry report).\nmax_z_mid_retries::Int: Companion to z_mid_retry_frac, analogous to max_path_steps (an attempt-count cap, not a tolerance): the number of perturbed-z_mid attempts _robust_slice_at_z makes before giving up and throwing a loud ErrorException naming the offending slab, rather than silently proceeding with (or looping forever trying to escape) a slice already known to contain an unexplained endpoint-fallback vertex. Default 8.\nz_mid_gradient_ratio_tol::T: Second, independent gate in _robust_slice_at_z, added after the vertex-type gate above was found (Taubin heart [-1,1] slab, retry investigation) to accept a topologically-clean candidate z_mid that was nonetheless still numerically ill-conditioned – decompose_1d_curve classified its curve cleanly (no stray Artificial/Singular vertices), but the surface's true gradient near that z_mid was orders of magnitude smaller than elsewhere on the same slab (a genuine near-degenerate NEIGHBORHOOD around the excluded exact critical/repeated-factor z-value, not just a single pathological point), which wrecked FaceTracking.track_face's Newton-based patch tracking downstream even though the raw 2D decomposition looked fine. A raw residual-magnitude threshold was tried first and rejected (see _robust_slice_at_z's own docstring): sample_edge's linear interpolation error has no clean scale-free gap to split. A same-point |∇_xy f| / |f_z| ratio was tried next and also rejected: it does not correlate with sweep quality at all (measured ratios for KNOWN-GOOD reference slabs, 0.60-1.16, sit BELOW several KNOWN-BAD candidates' ratios, 2.08-4.29, making it actively misleading, not merely noisy). What DOES work, reusing FaceTracking.patch_direction (the exact (a,b) pair track_face itself seeds each sweep with, per this field's own investigation) rather than any new gradient-computation logic: comparing a candidate z_mid's minimum per-edge anchor hypot(patch_direction(...)...) against a CROSS-Z reference computed at two FIXED, retry-schedule-independent locations well inside the same slab (z_bottom + 0.25*(z_top-z_bottom) and z_top - 0.25*(z_top-z_bottom), i.e. the slab's own quarter-points, taking the larger of the two magnitudes found there). This ratio is genuinely dimensionless (both numerator and denominator are the same hypot(patch_direction(...)...) quantity evaluated at different z-values, so it is invariant to the surface's own gradient scale) and showed a clean, wide gap on the Taubin heart: the two already-healthy reference slabs measured 0.82/0.98 (i.e. ~as strong as the slab's own quarter-point baseline), while the [-1,1] slab's bad candidates measured 0.0014-0.0057 and its eventually-accepted good candidate measured 0.013-0.023 – nearly two orders of magnitude of margin on both sides of the default 0.01 (1%) chosen here, not a threshold sitting uncomfortably close to either boundary. A new field, for the same reason z_mid_retry_frac is: no existing tolerance means \"is the local gradient strong enough relative to the rest of this slab\", and reusing one anyway would tie an unrelated knob's tuning to this different job. Known limitation, confirmed and left as-is rather than tuned around (see _robust_slice_at_z's own docstring): a curve with branches of very different local conditioning (e.g. a thin inner loop next to a strong outer loop) can trigger one harmless extra retry, since the gate compares a candidate's WEAKEST branch against the reference's STRONGEST branch.\n\n\n\n\n\n","title":"HomotopyGetsReal.HomotopyConfig"},{"category":"type","location":"api/#HomotopyGetsReal.VertexType","page":"API","text":"VertexType\n\nClassification of a NativeVertex within the pipeline.\n\nCritical: a critical point of the projection map (from compute_critical_points).\nBoundary: a point where a homotopy path crosses the bounding object (from intersect_bounding_object).\nSingular: a point where the Jacobian is rank-deficient beyond HomotopyConfig.jacobian_rank_tol/singular_value_threshold.\nArtificial: a synthetic vertex introduced by the pipeline itself (e.g. a merge/averaging result from Merge/GetMergeCandidates, or a slicing artifact) rather than one computed directly from the variety.\n\n\n\n\n\n","title":"HomotopyGetsReal.VertexType"},{"category":"type","location":"api/#HomotopyGetsReal.NativeVertex","page":"API","text":"NativeVertex{T<:AbstractFloat}\n\nA single vertex of the numerical decomposition, in complex space (as homotopy paths live in ℂⁿ even when we ultimately only care about the real points).\n\nFields\n\nid::Int: unique identifier used to reference this vertex from Edges and Faces without embedding it by value.\ncoordinates::Vector{Complex{T}}: the vertex's coordinates.\nv_type::VertexType: classification, see VertexType.\nmetadata::Dict{Symbol,Any}: intentionally untyped grab-bag for per-vertex bookkeeping that varies by vertex type and pipeline stage (e.g. :tolerance_used, :jacobian_rank, :source_slice, :singular_values, ...). Making this Dict{Symbol,Any} rather than a concrete field lets every step attach whatever diagnostic/derived information is convenient without forcing a schema change on NativeVertex itself. This is the only field allowed to be untyped; every other field in every struct in this file is concrete and parametric in T.\n\n\n\n\n\n","title":"HomotopyGetsReal.NativeVertex"},{"category":"type","location":"api/#HomotopyGetsReal.Edge","page":"API","text":"Edge{T<:AbstractFloat}\n\nA tracked path segment connecting two vertices, together with the points sampled along it – populated (raw) by ConnectTheDots!, refined to equidistant spacing by sample_edge.\n\nFields\n\nid::Int: unique identifier for this edge.\nleft_vertex_id::Int, right_vertex_id::Int: ids of the NativeVertex endpoints this edge connects.\nsampled_points::Vector{Vector{T}}: real-space points sampled along the edge. Deliberately Vector{Vector{T}} (a concrete, homogeneous vector-of-vectors) rather than Vector{Vector{Any}}, which was a bug in the old prototype that silently boxed every coordinate and defeated type inference downstream.\nis_singular::Bool: whether this edge passes through/near a singular locus (as opposed to being a smooth path).\n\n\n\n\n\n","title":"HomotopyGetsReal.Edge"},{"category":"type","location":"api/#HomotopyGetsReal.Face","page":"API","text":"Face{T<:AbstractFloat}\n\nA 2D cell of the decomposition, built from a mid-slice mesh (interslice/MidSlice!) and bounded by a set of Edges.\n\nFields\n\nid::Int: unique identifier for this face.\nmid_slice_z::T: the z-coordinate of the mid-slice this face was extracted from.\nboundary_edges::Vector{Int}: ids of the Edges that bound this face.\nmesh_vertices::Matrix{T}: mesh vertex coordinates, one row per vertex.\nmesh_topology::Matrix{Int}: mesh connectivity (e.g. triangle/quad index lists), one row per mesh element.\n\n\n\n\n\n","title":"HomotopyGetsReal.Face"},{"category":"function","location":"api/#HomotopyGetsReal.cluster_vertices","page":"API","text":"cluster_vertices(vertices::Vector{NativeVertex{T}}, tol::T) where {T<:AbstractFloat}\n    -> Vector{NativeVertex{T}}\n\nPartitions vertices into clusters by transitive closure of the relation \"within tol of each other\" (i.e. connected components of the graph where an edge connects any two vertices whose Euclidean coordinate distance is <= tol), and returns one representative NativeVertex{T} per cluster.\n\nThis is a general clustering primitive: it does not know anything about Critical/Boundary/Singular classification semantics beyond the conflict-resolution rule documented below, so it is safe for both Phase 2's straightforward dedup use and for a future, more elaborate Merge/GetMergeCandidates step (6-step framework, step 5) to build on top of.\n\nRepresentative construction\n\nFor each cluster:\n\nid: the smallest id among cluster members (deterministic and stable regardless of input order).\ncoordinates: the componentwise centroid (arithmetic mean) of all member coordinates.\nv_type: Singular if any member is Singular (a numerically detected singularity is never allowed to be \"averaged away\" by merging with a nearby regular point); else the common v_type if all members agree; else Artificial to flag that this vertex is a synthetic merge of members with genuinely different classifications.\nmetadata: built by merge_metadata — see its docstring for the exact merge rule. Nothing from any member is discarded.\n\nDistance is computed with LinearAlgebra.norm on the (complex) coordinate difference, so this works uniformly for T = Float64 and T = BigFloat.\n\n\n\n\n\n","title":"HomotopyGetsReal.cluster_vertices"},{"category":"function","location":"api/#HomotopyGetsReal.cluster_scalars","page":"API","text":"cluster_scalars(xs::Vector{T}, tol::T) where {T<:AbstractFloat}\n    -> Vector{T}\n\nSibling primitive to cluster_vertices, for the Phase 3 \"which x-values are actually distinct\" problem: same union-find-by- tolerance idea as cluster_vertices, collapsed to 1D (an edge connects x_i and x_j iff abs(x_i - x_j) <= tol), returning one representative (arithmetic mean) per connected component, sorted ascending.\n\nWhy a simple sorted adjacent-chain scan is enough (not O(n^2) union-find)\n\nFor 1D data, if a <= b <= c and abs(a - c) <= tol, then necessarily abs(a - b) <= tol and abs(b - c) <= tol too (since b lies between a and c). So any edge between two non-adjacent elements of the sorted sequence already implies edges between every intermediate adjacent pair. Consequently the connected components of the full \"within tol\" graph are exactly the maximal runs of the sorted sequence whose consecutive gaps are all <= tol – an O(n log n) sort plus a single O(n) scan, with no loss of generality relative to a full pairwise union-find (which is what cluster_vertices must use instead, since that equivalence does not hold in 2+ dimensions).\n\nThis is exactly the fix for the naive-adjacency counterexample: given the nodal cubic's boundary vertices at x ≈ 2.2268 (once at y = -4, once at y = +4), cluster_scalars collapses them into a single x-slot before decompose_1d_curve decides which intervals need a compute_midslice call, so the pipeline never mistakes \"same x\" for \"same vertex\" (that finer distinction is left to connect_the_dots!'s full (x, y) coordinate matching).\n\n\n\n\n\n","title":"HomotopyGetsReal.cluster_scalars"},{"category":"function","location":"api/#HomotopyGetsReal.cluster_points_indexed","page":"API","text":"cluster_points_indexed(points::Vector{Vector{T}}, tol::T) where {T<:AbstractFloat}\n    -> (representatives::Vector{Vector{T}}, membership::Vector{Int})\n\nPhase 5 primitive, needed by SurfaceDecomposition.weld_mesh for a job neither cluster_vertices nor cluster_scalars can do: cluster_vertices operates on NativeVertex{T}'s Complex{T} coordinates (mesh vertices are plain real T-valued rows of a Face.mesh_vertices matrix, not NativeVertex), and – more fundamentally – it returns only deduplicated representatives, never the mapping from each original input to the cluster it merged into. weld_mesh needs exactly that mapping to remap every face's mesh_topology (local row indices) into a single global vertex numbering (the mesh-index analogue of Phase 3's cross-source id renumbering, see SurfaceDecomposition.weld_mesh's own docstring).\n\nSame tolerance-indexed union-find-by-Euclidean-distance idea as cluster_vertices (an edge connects points[i] and points[j] iff norm(points[i] .- points[j]) <= tol, O(n^2) pairwise, exactly like cluster_vertices – no NativeVertex-specific metadata/v_type merge logic is needed here, so this is a strictly simpler sibling, not a generalization of cluster_vertices), but reports full provenance instead of discarding it:\n\nrepresentatives[k] is the componentwise centroid (arithmetic mean) of every points[i] in cluster k, in the order clusters are first encountered while scanning 1:n (deterministic given input order, unlike cluster_vertices's id-based sort – there is no id concept for raw points).\nmembership[i] is the index into representatives that points[i] merged into, for every i in 1:length(points), so callers can translate any original per-point reference (e.g. a local mesh-grid row index) into the deduplicated global numbering.\n\n\n\n\n\n","title":"HomotopyGetsReal.cluster_points_indexed"},{"category":"function","location":"api/#HomotopyGetsReal.jacobian_rank_info","page":"API","text":"jacobian_rank_info(F::System, point::AbstractVector, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> (rank::Int, singular_values::Vector{T})\n\nEvaluates the Jacobian of F at point (converted to Complex{T}) at T-precision (via low-level evaluate(...; bits = precision(T)), see module docstring above), computes its singular values with LinearAlgebra.svdvals (generic for any T once GenericLinearAlgebra is loaded), and reports the numerical rank obtained by counting singular values strictly greater than cfg.jacobian_rank_tol.\n\nThis is the shared utility used by both compute_critical_points and intersect_bounding_object; it does not itself decide Singular vs Critical/Boundary classification (see _classify_vertex_type, which additionally consults cfg.singular_value_threshold – a deliberately distinct knob from jacobian_rank_tol, see HomotopyConfig's docstring).\n\n\n\n\n\n","title":"HomotopyGetsReal.jacobian_rank_info"},{"category":"function","location":"api/#HomotopyGetsReal.compute_critical_points","page":"API","text":"compute_critical_points(F::System, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> Vector{NativeVertex{T}}\n\nStep 1 of the six-step framework.\n\nF must be either:\n\nalready square (length(F.expressions) == nvariables(F)), i.e. an already-augmented, 0-dimensional critical-point system – this is the curve case, matching the old prototype's convention where the caller pre-builds e.g. System([f, differentiate(f, y)], [x, y]) before calling in; or\na single equation in exactly 3 variables (a raw surface), in which case the augmented z-projection critical-point system {f, ∂f/∂x, ∂f/∂y} is built internally (mirrors prototipo_viejo_julia/Solver.jl's find_critical_z_slices).\n\nAny other shape throws ArgumentError.\n\nPipeline: path-track the (possibly augmented) system in Float64: filter to solutions whose imaginary part is within cfg.critical_point_tol (NOT cfg.vertex_match_tol); optionally Newton-polish to T-precision when T != Float64; classify each surviving point via jacobian_rank_info / _classify_vertex_type as Critical or Singular; deduplicate via Clustering.cluster_vertices using cfg.vertex_match_tol (NOT cfg.critical_point_tol – see HomotopyConfig's docstring for why these two must not be confused).\n\n\n\n\n\n","title":"HomotopyGetsReal.compute_critical_points"},{"category":"function","location":"api/#HomotopyGetsReal.intersect_bounding_object","page":"API","text":"intersect_bounding_object(F::System, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> Vector{NativeVertex{T}}\n\nStep 2 of the six-step framework.\n\nF must define a 1-dimensional variety in nvariables(F)-space, i.e. length(F.expressions) == nvariables(F) - 1 with nvariables(F) in (2, 3) – a plane curve (2 variables, matching the old prototype's find_boundary_points exactly) or a space curve (3 variables). A raw single-equation surface (1 equation, 3 variables) is out of scope here: a surface meets a bounding box face in a curve, not isolated points, so standard 0-dimensional homotopy continuation cannot return it as a Vector{NativeVertex}; that reduction belongs to per-slice processing in a later phase.\n\nFor each variable and each of its two cfg.bbox_* bounds, the variable is fixed at that bound, the resulting square system in the remaining variables is path-tracked in Float64, and solutions are kept if they are real (within cfg.boundary_point_tol, an imaginary-part cutoff) and their remaining coordinates fall within their own bounding intervals up to cfg.boundary_point_tol slack. Kept points are optionally Newton-polished (see _newton_polish) in the reduced variables, reassembled with the exact fixed coordinate, classified via the shared Jacobian-rank utility (evaluated against the original F, using all nvariables(F) variables, so the fixed coordinate's partial derivative is not lost), tagged Boundary (or Singular), and deduplicated via Clustering.cluster_vertices using cfg.vertex_match_tol.\n\n\n\n\n\n","title":"HomotopyGetsReal.intersect_bounding_object"},{"category":"function","location":"api/#HomotopyGetsReal.is_near_singular","page":"API","text":"is_near_singular(F::System, point::Vector{ComplexF64}, expected_rank::Int, rank_tol::Float64, sv_thresh::Float64)\n    -> Bool\n\nFloat64-only singularity probe used during Float64 path tracking (adaptive step-size reduction in track_path), as distinct from jacobian_rank_info (used elsewhere for genuine T-precision classification of already-matched endpoints). Since tracking itself is Float64-only by construction (see module docstring), there is nothing to gain from a bits-generic Jacobian evaluation here, so this uses HomotopyContinuation's own convenience jacobian(F, x) wrapper directly.\n\npoint must match F.variables in length and order (unlike Phase 3's _is_near_singular_f64, which hardcoded a 2-element [x, y] point, this works for any system shape).\n\nReturns true (i.e. \"treat this as a singularity, refine the step\") iff either:\n\nthe numerical rank of jacobian(F, point) (columns whose singular value exceeds rank_tol) falls short of expected_rank, or\nthe smallest singular value is below sv_thresh,\n\nexactly mirroring Solver._classify_vertex_type's two-part rule (rank-deficiency or near-zero smallest singular value), so mid-tracking singularity detection and post-hoc vertex classification agree on what \"singular\" means.\n\n\n\n\n\n","title":"HomotopyGetsReal.is_near_singular"},{"category":"function","location":"api/#HomotopyGetsReal.build_tracker","page":"API","text":"build_tracker(H_sys::System, x_start::Float64, cfg::HomotopyConfig{T}; compile::Symbol = :all) where {T<:AbstractFloat}\n    -> (ph::ParameterHomotopy, tracker::Tracker)\n\nConstructs the ParameterHomotopy/Tracker pair used by track_path/track_bidirectional, centralizing what Phase 3's connect_the_dots! previously built inline – so that every current and future caller (Phase 5's face-tracking included) gets consistent precision wiring for free.\n\nph is initialized with start_parameters = target_parameters = [x_start] (the caller is expected to move it via start_parameters!/ target_parameters! on each tracking step, exactly as before), built with compile = compile (default :all, matching Phase 3/4's original choice and every existing call site's behavior unchanged) rather than leaving HomotopyContinuation's own COMPILE_DEFAULT[] = :mixed heuristic in place: fixed(F; compile = :mixed) picks between InterpretedSystem/MixedSystem/CompiledSystem based on a runtime system-size heuristic, which makes ParameterHomotopy(H_sys, ...)'s own return type a Union of those three (confirmed via @inferred) – forcing any single fixed literal (:all or :none) instead always produces one single concrete system type, which is what makes build_tracker/track_path/track_bidirectional all @inferred-clean regardless of which literal is chosen (see the @inferred note below).\n\nWhy this is now a caller-chosen keyword (Phase 5 addition): Phase 3/4's curve-tracking call sites (connect_the_dots!) build one H_sys per curve and reuse it for every branch/direction of that same curve, so :all's one-time CompiledSystem-specialization cost is paid once and amortized – genuinely negligible, as originally documented. Phase 5's face-tracking (FaceTracking.build_face_tracker) instead builds a fresh, literal-coefficient H_sys per anchor point, per sweep direction (potentially hundreds to thousands per surface), so that one-time cost is paid over and over with no amortization. This was benchmarked directly (not guessed): across 15 structurally distinct systems, compile = :all's total construct+first-track cost was ~5,700x that of compile = :none (dominated by Julia re-specializing track's whole call graph against each new CompiledSystem type, not by HomotopyContinuation's own system-compilation step), while compile = :none's per-step tracking throughput on an already-warm system was not worse than :all's for the small (1-2 equation) systems this pipeline builds. Hence: Phase 3's existing call site keeps the default :all (unchanged behavior), and FaceTracking.jl's per-anchor construction explicitly passes compile = :none.\n\nNote on @inferred: even with compile fixed to a literal, build_tracker itself is not @inferred-clean – HomotopyContinuation.jl's own fixed/ParameterHomotopy dispatch chain branches on the runtime value of the compile keyword (compile == true || compile == :all, elseif compile == false || compile == :none, ...), not just its type (Union{Bool,Symbol}), so Core.Compiler cannot narrow past that if/elseif chain regardless of which literal we pass at any given call site; this is the identical category of upstream instability solve() itself has for the same reason, and it is identical for :all and :none alike – neither is \"more inferred\" than the other. This is a HomotopyContinuation.jl-internal limitation, not something build_tracker introduces or can paper over – see scratch_phase4_check.jl for a documented Core.Compiler.return_types inspection confirming this. Downstream callers are unaffected: track_path/track_bidirectional accept an already-constructed, concretely-typed tracker::Tracker instance, so those two functions remain fully @inferred-clean at their own call sites regardless of which compile literal built that tracker (verified below, and re-verified for compile = :none in scratch_phase5_check.jl).\n\ncfg.path_tracker_precision is threaded into TrackerOptions(min_step_size = Float64(cfg.path_tracker_precision)). This is the most defensible available mapping given HomotopyContinuation.jl's actual TrackerOptions fields (automatic_differentiation, max_steps, max_step_size, max_initial_step_size, extended_precision, min_step_size, min_rel_step_size, terminate_cond, parameters) – NOT an exact semantic match for \"requested precision\": HC exposes no literal requested-accuracy/requested-precision knob. Lowering min_step_size lowers the floor the tracker is allowed to shrink its adaptive step to before giving up (:terminated_step_size_too_small), which is the closest available lever to \"how precisely this tracker is willing to resolve a numerically difficult path\" – it does not guarantee any particular accuracy digit count.\n\n\n\n\n\n","title":"HomotopyGetsReal.build_tracker"},{"category":"function","location":"api/#HomotopyGetsReal.track_path","page":"API","text":"track_path(\n    F::System, ph::ParameterHomotopy, tracker::Tracker,\n    y_start::Vector{ComplexF64}, x_start::Float64, x_target::Float64,\n    max_steps::Int, rank_tol::Float64, sv_thresh::Float64, poor_acc_tol::Float64, min_width::Float64,\n) -> (y_final::Vector{ComplexF64}, path::Vector{Vector{Float64}})\n\nPublic, generalized replacement for Phase 3's _track_outward!: a thin wrapper that seeds a fresh budget = Ref(max_steps) (backed by cfg.max_path_steps, the total number of ParameterHomotopy steps this direction is allowed to take) and delegates to _track_path_segment!'s adaptive bisection.\n\nF is the original defining system (used only for is_near_singular's Jacobian evaluation – ph/tracker track the reduced/parameterized system built by the caller); expected_rank for that check is length(F.expressions), matching the convention already established in Solver.compute_critical_points/ intersect_bounding_object.\n\n\n\n\n\n","title":"HomotopyGetsReal.track_path"},{"category":"function","location":"api/#HomotopyGetsReal.track_bidirectional","page":"API","text":"track_bidirectional(\n    F::System, ph::ParameterHomotopy, tracker::Tracker,\n    y_mid::Vector{ComplexF64}, x_mid::Float64, x_left::Float64, x_right::Float64,\n    max_steps::Int, rank_tol::Float64, sv_thresh::Float64, poor_acc_tol::Float64, min_width::Float64,\n) -> (full_path::Vector{Vector{Float64}}, y_land_left::Vector{ComplexF64}, y_land_right::Vector{ComplexF64})\n\nPublic, generalized replacement for the bidirectional walk previously hand-rolled inline in Phase 3's connect_the_dots!: calls track_path from (x_mid, y_mid) toward x_left and x_right independently, and returns the already-combined reverse(path_left) ++ [[x_mid, real.(y_mid)...]] ++ path_right (the concatenation itself, not just the two halves, so callers no longer need to hand-roll it) plus both landing values, which the caller resolves against known vertices (curve-specific logic that stays in Topology._resolve_endpoint, not here).\n\n\n\n\n\n","title":"HomotopyGetsReal.track_bidirectional"},{"category":"function","location":"api/#HomotopyGetsReal.compute_midslice","page":"API","text":"compute_midslice(F::System, x_left::T, x_right::T, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> Vector{Complex{T}}\n\nStep 3 of the six-step framework (plane-curve specialization – see the module docstring for why this is not the 3D z-slicing operation of the same conceptual name in a later phase).\n\nF must be a raw plane curve: length(F.variables) == 2 && length(F.expressions) == 1 (same convention as intersect_bounding_object's raw-F input), else ArgumentError.\n\nFixes x_var (the first of F.variables, matching compute_critical_points's existing positional destructuring convention) at the midpoint x_mid = (x_left + x_right) / 2, path-tracks the resulting univariate system in y_var (the second variable) in Float64, and returns every root within cfg.critical_point_tol of real (the tolerance is reused, not reintroduced as a new HomotopyConfig field – see the Phase 3 architecture discussion) as a Vector{Complex{T}}.\n\nNo internal smoothness check is performed: this is guaranteed by construction as long as callers only ever invoke this strictly between two adjacent distinct x-values (see Clustering.cluster_scalars), never at a known vertex's own x.\n\n\n\n\n\n","title":"HomotopyGetsReal.compute_midslice"},{"category":"function","location":"api/#HomotopyGetsReal.connect_the_dots!","page":"API","text":"connect_the_dots!(\n    F::System,\n    x_left::T, x_mid::T, x_right::T,\n    y_mid::Complex{T},\n    edge_id::Int,\n    vertices::Vector{NativeVertex{T}},\n    cfg::HomotopyConfig{T},\n) where {T<:AbstractFloat}\n    -> Edge{T}\n\nStep 4 of the six-step framework. Called once per branch, i.e. once per y-root returned by compute_midslice at x_mid = (x_left + x_right) / 2.\n\nTracks outward from the smooth midslice witness (x_mid, y_mid) toward x_left and x_right independently via build_tracker/ track_bidirectional (Phase 4's PathTracking.jl engine, parameterized by x_var), using cfg.max_path_steps as the raw tracking step budget for each direction (adaptive bisection near detected singularities/failures – see _track_path_segment! – still bounded overall by this budget; cfg.edge_sample_density is never used here, it is reserved exclusively for sample_edge's later equidistant resampling).\n\nAt each end, the landing point is resolved via _resolve_endpoint against the full vertices list by actual (x, y) distance (cfg.vertex_match_tol) – never by position in a sorted list (the Phase 3 naive-adjacency counterexample). vertices is mutated in place (hence the !) whenever a tracked branch lands on no known vertex: a new Artificial-typed NativeVertex{T} is appended so later calls in the same decompose_1d_curve run see it too.\n\nedge_id is supplied by the caller; connect_the_dots! never self-assigns edge ids. Edge.is_singular is true iff either resolved endpoint vertex (matched or newly-created) has v_type == Singular. The returned Edge{T} has raw (non-equidistant) sampled_points already populated, running left-endpoint -> ... -> midpoint -> ... -> right-endpoint.\n\n\n\n\n\n","title":"HomotopyGetsReal.connect_the_dots!"},{"category":"function","location":"api/#HomotopyGetsReal.sample_edge","page":"API","text":"sample_edge(edge::Edge{T}, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> Edge{T}\n\nStep 6 of the six-step framework: pure geometric resampling, with zero HomotopyContinuation.jl involvement (it takes no System/vertices argument, unlike every other function in this file). Arc-length parametrizes edge.sampled_points and linearly interpolates to exactly cfg.edge_sample_density equidistant points, returning a new Edge{T} with the same id/left_vertex_id/right_vertex_id/is_singular and replaced sampled_points.\n\nDegenerate inputs (fewer than 2 raw points, or all raw points coincident, e.g. a zero-length edge) are handled by simply repeating the single available point cfg.edge_sample_density times, rather than dividing by a zero total arc length.\n\n\n\n\n\n","title":"HomotopyGetsReal.sample_edge"},{"category":"function","location":"api/#HomotopyGetsReal.decompose_1d_curve","page":"API","text":"decompose_1d_curve(F::System, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> (vertices::Vector{NativeVertex{T}}, edges::Vector{Edge{T}})\n\nOrchestrator for the full \"1D Curve Decomposition Pipeline\" (steps 3-6, following on from Phase 2's steps 1-2). Takes the raw plane curve system (same validation as compute_midslice): length(F.variables) == 2 && length(F.expressions) == 1.\n\nBuilds the augmented critical-point system System([f, differentiate(f, y_var)], [x_var, y_var]) and calls compute_critical_points on it; calls intersect_bounding_object on F directly.\nRenumbers the second vertex set's ids past the first set's maximum id (their internal numbering otherwise collides, since each function starts counting at 1 independently), concatenates both sets, and calls Clustering.cluster_vertices(..., cfg.vertex_match_tol) – the actual cross-source \"GetMergeCandidates\" step (step 5).\nCalls Clustering.cluster_scalars on the merged vertices' real x-coordinates to obtain the distinct x-slots that need connecting (see the Phase 3 naive-adjacency counterexample for why this step, rather than naively sorting+adjacent-pairing raw vertices, is required).\nFor each adjacent pair of distinct x-slots, calls compute_midslice once, then connect_the_dots! once per returned y-root, using a locally-owned incrementing edge_id counter. vertices is threaded through by reference so in-place Artificial-vertex insertions from one interval are visible to later intervals processed in the same call.\nRuns sample_edge over every produced edge.\n\n\n\n\n\n","title":"HomotopyGetsReal.decompose_1d_curve"},{"category":"function","location":"api/#HomotopyGetsReal.build_patch_system","page":"API","text":"build_patch_system(F::System)\n\nPhase 5 entry point for the patch machinery: one-time, per-surface, purely SYMBOLIC preparation – takes only the raw surface, no anchor point (evaluating the gradient at a specific anchor is a separate, per-anchor responsibility, see patch_direction/ _gradient_at).\n\nF must be a raw surface: length(F.variables) == 3 && length(F.expressions) == 1, with F.variables ordered [x_var, y_var, z_var] (z LAST) – the same convention compute_critical_z_slices/Solver.compute_critical_points's 3-variable branch already requires (x, y, _ = F.variables), since getting this order backwards silently computes critical points of the wrong projection with no error thrown.\n\nComputes ∂f/∂x, ∂f/∂y, ∂f/∂z once via differentiate (cheap, symbolic, not per-anchor), and also builds F_for_tracking – the SAME expression f, but wrapped in a second System with variables reordered to [z_var, x_var, y_var] (z FIRST). This second ordering is required purely so that PathTracking._track_path_segment!'s hardcoded point = vcat(ComplexF64(x1), y1) convention (swept parameter first, then state – exactly mirroring the plane-curve case where x_var IS F.variables[1]) lines up correctly when the swept parameter is z and the state is (x,y). This is entirely independent from, and does not change, the [x_var,y_var,z_var] order F itself must keep for compute_critical_z_slices's own augmentation convention – the two Systems serve different, non-interacting purposes (F_for_tracking is used only by is_near_singular's rank check on the genuine surface, via track_dense_path; the per-anchor H_sys built by build_face_tracker is what's actually numerically tracked).\n\nReturns a NamedTuple with fields f, fx, fy, fz, x_var, y_var, z_var, F_for_tracking.\n\n\n\n\n\n","title":"HomotopyGetsReal.build_patch_system"},{"category":"function","location":"api/#HomotopyGetsReal.patch_direction","page":"API","text":"patch_direction(patch::NamedTuple, x0::T, y0::T, z0::T, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> (a::T, b::T)\n\nConfirmed decision: the per-anchor patch equation a*(x-x0) + b*(y-y0) = 0 uses the LOCAL SURFACE GRADIENT direction (a,b) = (f_y(x0,y0,z0), -f_x(x0,y0,z0)) at the anchor – i.e. the line through the anchor orthogonal to (f_x,f_y)'s in-plane component – rather than the old prototype's origin-radial line x*y0 - y*x0 = 0 (prototipo_viejo_julia/SurfaceTopology.jl:39), which degenerates for curves not centered on the origin. This is the standard \"coordinate patch\" trick from numerical algebraic geometry (Sommese-Wampler-style witness-point tracking): a transversal line through the anchor is guaranteed valid at the anchor itself by construction (the patch equation is tautologically satisfied there), and the gradient direction maximizes the local intersection angle with the curve, minimizing the chance the patch itself introduces spurious near-tangency.\n\n\n\n\n\n","title":"HomotopyGetsReal.patch_direction"},{"category":"function","location":"api/#HomotopyGetsReal.build_face_tracker","page":"API","text":"build_face_tracker(patch::NamedTuple, x0::T, y0::T, z0::T, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> (H_sys::System, ph::ParameterHomotopy, tracker::Tracker)\n\nOption B (confirmed): builds a FRESH, per-anchor System with x0,y0,a,b baked in as LITERAL Float64 coefficients (a,b from patch_direction at (x0,y0,z0)), leaving only z_var as the system's ParameterHomotopy parameter – so this requires no changes to PathTracking.jl's existing scalar x_start/x_target contract. Constructed via build_tracker(...; compile = :none) (src/PathTracking.jl's Phase 5 addition): per the benchmark documented on build_tracker, baking literals per-anchor (rather than threading x0,y0,a,b as extra homotopy parameters and building once) is only feasible with compile = :none, since this function is called once per anchor point (potentially hundreds to thousands of times per surface).\n\n\n\n\n\n","title":"HomotopyGetsReal.build_face_tracker"},{"category":"function","location":"api/#HomotopyGetsReal.track_dense_path","page":"API","text":"track_dense_path(\n    F::System, ph::ParameterHomotopy, tracker::Tracker,\n    y_start::Vector{ComplexF64}, param_start::Float64, param_targets::Vector{Float64},\n    max_steps_per_hop::Int, rank_tol::Float64, sv_thresh::Float64, poor_acc_tol::Float64, min_width::Float64,\n) -> (y_final::Vector{ComplexF64}, dense_path::Vector{Vector{Float64}})\n\nNew sibling to PathTracking.track_path (see this file's module docstring for why track_path itself is unsuited to mesh-building): walks param_targets IN ORDER, calling PathTracking._track_path_segment! once per consecutive pair (param_start_or_previous_target, param_targets[i]) into a FRESH, per-hop scratch buffer – reusing the EXACT SAME adaptive retry-on-poor-quality/is_near_singular bisection track_path uses, just applied between many small hops instead of once across the whole interval. Regardless of how much intra-hop bisection fires, the LAST point _track_path_segment! ever pushes for a given hop is always exactly at that hop's own target (bisection only ever inserts EARLIER points before the final landing, never changes what the final landing value is computed at) – so track_dense_path keeps only that final per-hop point, guaranteeing dense_path has exactly length(param_targets) entries, one per requested target, unlike track_path's endpoint-only accumulation (which may produce anywhere from 1 point up to the full recursion depth for a single interval).\n\nF is the raw surface's Jacobian-check system (i.e. patch.F_for_tracking from build_patch_system, NOT the per-anchor augmented H_sys from build_face_tracker) – is_near_singular checks genuine surface singularity, deliberately decoupled from whether the artificial patch equation happens to be poorly conditioned at some intermediate point.\n\n\n\n\n\n","title":"HomotopyGetsReal.track_dense_path"},{"category":"function","location":"api/#HomotopyGetsReal.track_face","page":"API","text":"track_face(\n    F::System, patch::NamedTuple, edge::Edge{T},\n    z_mid::T, z_bottom::T, z_top::T, face_id::Int, cfg::HomotopyConfig{T},\n) where {T<:AbstractFloat}\n    -> Face{T}\n\nSweeps an entire Edge{T} (already resampled to cfg.edge_sample_density points by Phase 3's sample_edge) across the full slab [z_bottom, z_top], assembling one Face{T} whose mesh_vertices is a (2*cfg.midslice_sample_density + 1) x cfg.edge_sample_density grid of points all lying on the surface (f ≈ 0 by construction, since every row comes from a tracked point of patch.f).\n\nRow/column indexing convention (fixed, see architecture resolution)\n\nColumns c = 1..edge_sample_density index edge.sampled_points (curve arclength order, unchanged from Phase 3).\nRows r = 1..n_z index the combined z-sweep, n_z = 2*n_side + 1 where n_side = cfg.midslice_sample_density: row 1 is z_bottom, row n_side+1 is the EXACT z_mid anchor (not a tracked value – the anchor coordinates themselves), row n_z is z_top, built as reverse(dense_path_down) ++ [mid_row] ++ dense_path_up – the direct z-axis analogue of track_bidirectional's own reverse(path_left) ++ [midpoint] ++ path_right combination. sweep_face_bidirectional's dense paths are [z,x,y]-ordered (matching _track_path_segment!'s vcat(swept_param, state) convention); each row is reordered to [x,y,z] here, matching this codebase's standing coordinate convention, before being stored.\nFlat storage is row-major: point (r,c) lives at mesh_vertices[(r-1)*edge_sample_density + c, :].\n\nEach edge.sampled_points[c] is first re-projected onto the actual slice curve at z_mid via _project_to_slice before being used as a sweep anchor – see that function's docstring for why this is necessary (Phase 3's sample_edge output is only an approximate, linearly-interpolated polyline, not literally on the curve).\n\nTriangulation convention (fixed, see architecture resolution)\n\nFor each grid cell with corners v1=(r,c), v2=(r+1,c), v3=(r+1,c+1), v4=(r,c+1), the diagonal always runs v1-v3, splitting into (v1,v2,v3) and (v1,v3,v4) (matching prototipo_viejo_julia/SurfaceTopology.jl:211-224's existing convention). Triangles degenerate under THIS face's own local indices (e.g. two corners of a swept quad coincide from a pinch) are dropped here; winding-order normalization (aligning normals with ∇f) is deliberately NOT done here – it happens once, globally, in SurfaceDecomposition.weld_mesh, after cross-face index remapping, so that every triangle in the finished mesh (not just within one face) is consistently oriented.\n\n\n\n\n\n","title":"HomotopyGetsReal.track_face"},{"category":"function","location":"api/#HomotopyGetsReal.sweep_face_bidirectional","page":"API","text":"sweep_face_bidirectional(\n    F::System, patch::NamedTuple,\n    x0::T, y0::T, z_mid::T, z_bottom::T, z_top::T,\n    cfg::HomotopyConfig{T},\n) where {T<:AbstractFloat}\n    -> (dense_path_down::Vector{Vector{Float64}}, dense_path_up::Vector{Vector{Float64}})\n\nThe z-sweep analogue of PathTracking.track_bidirectional: sweeps ONE anchor (x0,y0) from z_mid toward z_bottom and toward z_top, each via _sweep_direction with cfg.midslice_sample_density intermediate targets EXCLUDING the shared start (z_mid) and INCLUDING the far end (z_bottom/z_top respectively, tracked directly, no artificial pullback margin – see sweep_face_bidirectional's dropped-epsilon rationale below).\n\nEach direction independently starts from the SAME anchor (x0,y0,z_mid) and may adaptively re-anchor partway through its own sweep (see _sweep_direction) – the two directions never share a tracker instance (each calls build_face_tracker freshly), so there is no stale-state carryover between them.\n\n_sweep_direction's internal re-anchoring calls build_face_tracker (hence build_tracker(...; compile = :none)) an adaptive, usually-small number of times per direction – confirmed empirically fully @inferred-clean end-to-end in scratch_phase5_check.jl despite this, for the same union-splitting reason documented on build_tracker itself.\n\nNo ε pullback from z_bottom/z_top. Phase 3's connect_the_dots!/track_bidirectional already track directly to genuinely critical/singular target values with no pullback (e.g. scratch_phase4_check.jl tracks directly to a real node at x=0 and converges), relying entirely on is_near_singular's bisection retry. z_bottom/z_top play exactly the same role here (each is, by construction, either a genuine z-critical value from compute_critical_z_slices or a cfg.bbox_z boundary) – there is no principled reason the z-sweep needs a safety margin the already-proven 1D engine doesn't, and introducing one would reintroduce exactly the kind of unconfigured, ad-hoc literal (prototipo_viejo_julia/SurfaceTopology.jl:46's ε = 1e-4) this rebuild exists to eliminate. _sweep_direction's adaptive re-anchoring is the mechanism that makes tracking all the way to a genuine limiting point (like a pole) actually work on general surfaces, rather than merely failing gracefully.\n\n\n\n\n\n","title":"HomotopyGetsReal.sweep_face_bidirectional"},{"category":"function","location":"api/#HomotopyGetsReal.compute_critical_z_slices","page":"API","text":"compute_critical_z_slices(F::System, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> Vector{T}\n\nFinds the z-values at which the surface F cannot be locally expressed as a smooth graph z = g(x,y) – i.e. where ∂f/∂x = ∂f/∂y = 0 along with f = 0 – via Solver.compute_critical_points's existing 3-variable branch (src/Solver.jl, ne == 1 && nv == 3), which already builds exactly this augmented system internally. No new solver logic.\n\nF.variables must be ordered [x_var, y_var, z_var], z LAST – compute_critical_points's 3-variable branch destructures x, y, _ = F.variables and differentiates w.r.t. the first two, so getting this order backwards silently computes critical points of the wrong projection with no error thrown. This is the same requirement build_patch_system has, and both must agree on the same F.\n\nDeduplicates the resulting critical points' z-coordinates via Clustering.cluster_scalars(z_values, cfg.vertex_match_tol) – reusing the exact same primitive and tolerance Topology.decompose_1d_curve already uses for its own \"which x-values are actually distinct\" problem, rather than reintroducing the old prototype's bare cluster_points_1d(z_values, 1e-5) literal (prototipo_viejo_julia/Solver.jl:86).\n\nReturns a sorted Vector{T} of distinct z-values.\n\n\n\n\n\n","title":"HomotopyGetsReal.compute_critical_z_slices"},{"category":"function","location":"api/#HomotopyGetsReal.slice_at_z","page":"API","text":"slice_at_z(F::System, z_val::T, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> (vertices_3d::Vector{NativeVertex{T}}, edges_3d::Vector{Edge{T}})\n\nSubstitutes z_var => Float64(z_val) into the raw surface F to get a 2-variable curve, then calls Topology.decompose_1d_curve as-is (confirmed in the Phase 5 investigation: decompose_1d_curve's own signature already accepts exactly this shape, zero Phase 3 changes needed), and lifts the resulting 2D vertices/edges into 3-space by appending z_val as a third coordinate to every NativeVertex coordinate and every Edge sampled point.\n\nThis lifting is possible with zero Types.jl changes because NativeVertex{T}.coordinates/Edge{T}.sampled_points were already built length-generic in Phase 1 (confirmed: their keyword constructors just do Vector{Complex{T}}(undef, length(coordinates))/collect(p), no hardcoded length-2 assumption).\n\nNote: the returned vertex/edge ids are LOCAL to this one call (Phase 3's decompose_1d_curve restarts its own id counters near 1 every call) – callers concatenating results across multiple slice_at_z calls (i.e. decompose_3d_surface) MUST renumber past the running max before combining, exactly as decompose_3d_surface does.\n\n\n\n\n\n","title":"HomotopyGetsReal.slice_at_z"},{"category":"function","location":"api/#HomotopyGetsReal.decompose_3d_surface","page":"API","text":"decompose_3d_surface(F::System, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> (vertices::Vector{NativeVertex{T}}, edges::Vector{Edge{T}}, faces::Vector{Face{T}}, mesh::GeometryBasics.Mesh)\n\nTop-level Phase 5 orchestrator, the 3D analogue of Topology.decompose_1d_curve: computes critical z-slices, slices\n\ndecomposes + sweeps each resulting slab, and welds the result into a\n\nsingle mesh – running every pipeline step including the final weld, so callers never need a manual follow-up call, mirroring decompose_1d_curve's own precedent of calling sample_edge internally before returning.\n\nF.variables must be ordered [x_var, y_var, z_var], z LAST (see compute_critical_z_slices/build_patch_system).\n\nz_crits = compute_critical_z_slices(F, cfg), filtered to cfg.bbox_z's own range (a critical z-value outside the configured bounding box is not a slab boundary this decomposition should ever visit).\nz_bounds = sort(unique([cfg.bbox_z[1]; z_crits; cfg.bbox_z[2]])).\npatch = build_patch_system(F) – ONCE for the whole surface, passed both to _robust_slice_at_z (which reuses FaceTracking.patch_direction for its gradient-magnitude gate – see that function's own docstring) and to FaceTracking.track_face below.\nFor each adjacent (z_bottom, z_top) pair: _robust_slice_at_z at the exact midpoint (z_bottom + z_top) / 2 (matching Topology.compute_midslice's own convention – the old prototype's 0.4137 skew (prototipo_viejo_julia/SurfaceTopology.jl:132) is deliberately dropped, not ported to a new config field), falling back to a nearby perturbed z_mid only if the exact midpoint's slice comes back untrustworthy by either its vertex-type or gradient-magnitude gate (see _robust_slice_at_z's own docstring for when/why), then renumber that slab's vertex/edge ids past the running max (see below) before calling FaceTracking.track_face once per resulting edge, using the SAME z_mid that _robust_slice_at_z actually used (not blindly re-deriving (z_bottom+z_top)/2), so the z-value sliced and the z-value swept-from always agree even when a retry fired.\nweld_mesh over every produced Face.\n\nVertex/edge id renumbering across slabs (explicit, not implied)\n\nEvery slice_at_z call restarts decompose_1d_curve's own id counters near 1, so simply concatenating each slab's vertices_2d/ edges_2d WOULD produce colliding ids across DIFFERENT slabs – not a \"these are really the same point\" merge candidate the way Phase 3's crit_vertices/bnd_vertices were (every slab's z_mid is distinct, so no two slabs' vertices are ever the same physical point), but a pure NAMESPACE collision that would break any downstream findfirst(v -> v.id == ..., vertices) lookup (the pattern Topology.jl itself uses throughout) against the concatenated list. Fixed by offsetting BOTH id namespaces independently past their own running max before concatenating – vertex ids and edge ids are tracked as two separate counters, exactly as decompose_1d_curve itself already keeps them separate within one call – and shifting left_vertex_id/ right_vertex_id by the SAME vertex offset so they stay consistent with the renumbered vertices. Face.boundary_edges is populated from the already-renumbered edge, so it dereferences correctly against the returned edges. This is the exact structural analogue of Phase 3's own offset = maximum(v.id for v in crit_vertices) pattern (src/Topology.jl:358), applied across slab calls instead of within one call, and applied to two namespaces instead of one.\n\n\n\n\n\n","title":"HomotopyGetsReal.decompose_3d_surface"},{"category":"function","location":"api/#HomotopyGetsReal.weld_mesh","page":"API","text":"weld_mesh(faces::Vector{Face{T}}, patch::NamedTuple, cfg::HomotopyConfig{T}) where {T<:AbstractFloat}\n    -> GeometryBasics.Mesh\n\nFinal assembly step: merges coincident vertices across every Face's own, independently-numbered mesh_vertices/mesh_topology into one watertight mesh. Each Face.mesh_topology holds indices LOCAL to that face's own mesh_vertices – adjacent slabs' faces genuinely share their boundary row (one slab's z_top sweep and the next slab's z_bottom sweep both land on the same physical curve), so unlike decompose_3d_surface's vertex/edge id collision (a pure namespace problem, fixed by renumbering, since no two slabs' curve vertices are ever the same physical point), this genuinely needs POSITION-BASED clustering, not just an offset.\n\nExplicit four-step procedure\n\nFlatten with provenance. Every face's mesh_vertices rows are pushed into one flat all_points::Vector{Vector{T}}, recording provenance[k] = (face_idx, local_row) for each flattened point.\nCluster globally. reps, membership = Clustering.cluster_points_indexed(all_points, cfg.vertex_match_tol) – reps becomes the merged mesh's global vertex list; membership maps each flattened point to its global index.\nBuild the (face_idx, local_row) -> global_idx lookup from provenance + membership.\nRemap every face's mesh_topology through the lookup, dropping any triangle whose three remapped indices are not all distinct (legitimate: a swept grid can pinch to a point at a pole/tip, exactly as the old prototype already guards against at prototipo_viejo_julia/SurfaceTopology.jl:217-224).\n\nWinding-order normalization\n\nDone HERE, globally, after remapping and degenerate-triangle filtering (deliberately NOT in FaceTracking.track_face, which only emits the fixed (v1,v2,v3)/(v1,v3,v4) diagonal split with no orientation guarantee – different edges are discovered independently by Phase 3's midslice-first algorithm with no shared global orientation convention, so a per-face fix could not guarantee cross-face consistency). For each surviving triangle (g1,g2,g3), computes the raw normal n = (p2-p1) x (p3-p1) and the surface gradient ∇f = (fx,fy,fz) evaluated at p1 (this specific vertex was chosen, not the centroid, per the confirmed resolution: p1 is already at hand from the cross product computation, and a well-resolved mesh makes any one corner equally representative of the local gradient direction for a pure sign check). If n . ∇f < 0, the triangle is emitted as (g1,g3,g2) (swapped winding); otherwise as (g1,g2,g3). This gives every triangle in the finished mesh a normal consistently aligned with +∇f (an arbitrary but globally fixed convention).\n\nWelds at genuine T-precision (see this file's module docstring for why, contrasting with the old prototype's Float32 truncation before its own dedup decision); only narrows to Float32/Point3f for the final GeometryBasics.Mesh container.\n\nNot @inferred-clean – but for a different reason than build_face_tracker's documented Union: GeometryBasics.Mesh's own constructor return type carries an existential where _A type parameter (its internal view-storage type), which Core.Compiler cannot resolve away regardless of how concretely-typed weld_mesh's own inputs are. Confirmed via Base.return_types in scratch_phase5_check.jl; an upstream GeometryBasics.jl property, not something fixable here.\n\n\n\n\n\n","title":"HomotopyGetsReal.weld_mesh"},{"category":"function","location":"api/#HomotopyGetsReal.plot_curve_decomposition","page":"API","text":"plot_curve_decomposition(\n    vertices::Vector{NativeVertex{T}},\n    edges::Vector{Edge{T}};\n    cfg::Union{Nothing,HomotopyConfig} = nothing,\n    show_vertices::Bool = true,\n    show_labels::Bool = false,\n    edge_color_by::Symbol = :cell,\n    edge_color = :steelblue,\n    colormap = :viridis,\n) where {T<:AbstractFloat}\n    -> Makie.Figure\n\nRenders a 2D curve decomposition – the direct output of Topology.decompose_1d_curve or SurfaceDecomposition.slice_at_z (both already return exactly (vertices, edges) in this shape; no adapter needed).\n\ncfg, if given, sets axis limits from cfg.bbox_x/cfg.bbox_y rather than Makie's auto-fit-to-data, mirroring BertiniReal's own _adjust_axis_bounds (itself derived from the same kind of known-bounding-region config the decomposition was computed against).\nedge_color_by: :cell (default, BertiniReal's BY_CELL) gives every edge its own color cycled from colormap; :mono uses a flat edge_color for every edge.\nshow_vertices: one legended scatter! per VertexType present, via _plot_vertices_by_type!.\nshow_labels: annotates each vertex with its id via text!.\n\nReturns the Figure WITHOUT calling display on it – composable with a caller's own layout, or save(path, fig), without forcing a window open. Contrast interactive_3d_viewer, whose entire contract IS opening a window.\n\n\n\n\n\n","title":"HomotopyGetsReal.plot_curve_decomposition"},{"category":"function","location":"api/#HomotopyGetsReal.plot_surface_decomposition","page":"API","text":"plot_surface_decomposition(\n    mesh::GeometryBasics.Mesh;\n    color_by::Union{Symbol,Function} = :z,\n    colormap = :viridis,\n    show_wireframe::Bool = false,\n    show_colorbar::Bool = true,\n    vertices::Union{Nothing,Vector{<:NativeVertex}} = nothing,\n    cfg::Union{Nothing,HomotopyConfig} = nothing,\n) -> Makie.Figure\n\nPRIMARY 3D surface plotting method: takes the ALREADY-WELDED, ALREADY-correctly-wound mesh SurfaceDecomposition.decompose_3d_surface returns (via SurfaceDecomposition.weld_mesh) – not faces alone. See this file's header for why the winding correction makes mesh the right primary input; the faces::Vector{Face{T}} method below is the explicitly-flagged secondary path.\n\ncolor_by: :x/:y/:z extract that coordinate per mesh point (:z, the default, matches the roadmap's own sketch); a Function (x,y,z) -> Real generalizes this to BertiniReal's BY_FUNCTION (e.g. distance from origin, or any other diagnostic of interest) at negligible extra cost.\nshow_colorbar: adds a Colorbar for the (continuous) color_by scale.\nshow_wireframe: overlays the mesh's own triangle edges.\nvertices: optional overlay (e.g. the surface's own critical/singular points from decompose_3d_surface's first return value) via the SAME _plot_vertices_by_type! helper plot_curve_decomposition uses – the 3D analogue of BertiniReal overlaying critical_curve/ singular_curves on a raw surface.\ncfg, if given, sets axis limits from cfg.bbox_x/cfg.bbox_y/ cfg.bbox_z, mirroring plot_curve_decomposition's own convention.\n\nIf color_by's range across mesh is near-constant relative to its own magnitude (see _near_constant_colorrange), a fixed colorrange is used instead of Makie's default auto-scaling, and a one-shot @warn is emitted – prevents floating-point round-off on an otherwise-correct mesh from rendering as misleading full-spectrum speckle (see this file's own Phase 6 follow-up investigation for the motivating case).\n\nReturns the Figure without calling display (see plot_curve_decomposition's docstring for why).\n\n\n\n\n\nplot_surface_decomposition(\n    faces::Vector{Face{T}};\n    colormap = :viridis,\n    show_wireframe::Bool = false,\n    cfg::Union{Nothing,HomotopyConfig} = nothing,\n) where {T<:AbstractFloat} -> Makie.Figure\n\nSECONDARY 3D surface plotting method (multiple dispatch on the same exported name, not a keyword flag – see this file's header): BertiniReal's \"color by cell\" view, one solid, distinct color per Face (cycled from colormap via Makie.cgrad(colormap, n; categorical = true), exactly like plot_curve_decomposition's edge_color_by = :cell default), useful for visually distinguishing which triangles belong to which topological cell – information weld_mesh's globally-welded output has already discarded.\n\nKnown, deliberately-flagged limitation: unlike the mesh method above, this path builds each face's local mesh directly from face.mesh_vertices/face.mesh_topology and does NOT reproduce weld_mesh's gradient-based winding correction (that correction needs patch/F, which this method never receives – see this file's header). Some faces may therefore render with inverted/inward-facing normals under directional lighting. This is flagged via an explicit, one-time @warn on first call (not merely a docstring note, and not a per-call warning either – a module-level Ref{Bool} latch), since a visually-inverted face could otherwise look like a rendering bug rather than a known, documented limitation of this specific path.\n\n\n\n\n\n","title":"HomotopyGetsReal.plot_surface_decomposition"},{"category":"function","location":"api/#HomotopyGetsReal.interactive_3d_viewer","page":"API","text":"interactive_3d_viewer(\n    mesh::GeometryBasics.Mesh;\n    color_by::Union{Symbol,Function} = :z,\n    colormap = :viridis,\n    show_wireframe::Bool = false,\n    show_colorbar::Bool = true,\n    vertices::Union{Nothing,Vector{<:NativeVertex}} = nothing,\n    cfg::Union{Nothing,HomotopyConfig} = nothing,\n) -> Makie.Figure\n\nThin wrapper around plot_surface_decomposition's mesh method (reused internally, not duplicated) that activates the GLMakie backend and displays the resulting Figure before returning it – unlike plot_surface_decomposition/plot_curve_decomposition, whose entire contract is composability (return a Figure, never force a window), THIS function's entire contract is \"open an interactive window\", so forcing display here is a deliberate, documented asymmetry.\n\nRotation/zoom come for free from GLMakie's default Axis3 camera – no custom trackball code needed (unlike BertiniReal's separate, hand-written glumpy/OpenGL viewer, which duplicates its own matplotlib renderer's data-extraction logic just to get free camera controls).\n\nA live/interactive re-slicing control (dragging a z-value to recompute a NEW cross-section on the fly) is explicitly OUT of scope here – see this file's header; BertiniReal's own \"slice exploration\" only toggles visibility of already-computed curves, never recomputes one.\n\n\n\n\n\n","title":"HomotopyGetsReal.interactive_3d_viewer"},{"category":"section","location":"#HomotopyGetsReal","page":"Home","text":"using Markdown\nreadme = read(joinpath(@__DIR__, \"..\", \"..\", \"README.md\"), String)\n# Drop the leading H1 so Documenter's page title is not duplicated.\nreadme = replace(readme, r\"^# HomotopyGetsReal\\r?\\n+\" => \"\"; count = 1)\nMarkdown.parse(readme)","title":"HomotopyGetsReal"},{"category":"section","location":"#Documentation-pages","page":"Home","text":"Pages = [\"api.md\"]\nDepth = 2","title":"Documentation pages"}]
}
